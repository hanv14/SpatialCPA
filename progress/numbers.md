# Numbers the paper needs

Part of [PROGRESS.md](../PROGRESS.md).

## Numbers the paper needs (fill in as tasks land)

| Quantity | Source task | Value |
|---|---|---|
| Text/co-expression Spearman (synthetic, then real) | T02 | synthetic: **+0.0055** (≈ 0, as expected — arbitrary gene names); real: pending a real panel + `resources/gene_meta.parquet` |
| GRF vs i.i.d. Moran's I error ratio | T03 | **0.130** (median \|I_gen − I_real\|: GRF 0.0552, i.i.d. 0.4233); Geary's C ratio 0.130; per-gene r 0.917 (GRF) vs 0.377 (i.i.d.). Gate fixture, 3000 µm FOV |
| Selected configuration (synthetic fixture) | T09 | **`hybrid` + `correlated` + `zinb-flow` + `medcpt`, `train_steps=2400`, all three metric weights 0.5** — hash `00ef4a19a2f576b8`, 23 fits, 21 330 s. Chosen by the 18-cell full-budget gate that `specs/09` §3's training-free-option rule requires; all six `cross-mix` cells rank bottom (13.0–17.0 of 18) and all three merged gates changed answer against the old reduced-budget selection (`field` + `iid` + `cross-mix` + `medcpt`, `fe49ea9f8ad54bb2`). Definition of done: method **0.9517 / 0.9142 / 0.9730 / −0.0425 / 0.0519 / −0.0660** vs `resample` 0.8278 / 0.7683 / 0.6844 / −0.0374 / 0.0195 / −0.0660 and independent-donor 0.8716 / 0.8273 / 0.7388 / −0.0404 / 0.0405 / −0.0660 — **4 of 6 against both**, one tie, one loss. `text_emb_mode` was re-scored at the selected budget under the widened rule and `lookup` won on rank (1.2 vs 1.8) by less than the reproducibility envelope, which 3 cells x 3 seeds then measured at **0.0335** — not score-dependent, 5x metric-dependent. Against a 0.04 threshold **two** gates were decided inside the noise: `text_emb_mode` (0.0110) and, unsuspected, **`layout_mode`** (0.0344), both resolved by the capability tie-break to `medcpt` and `hybrid` (R10). **T10 benchmarks this config**; the pre-R8 numbers are superseded |
| **`text_emb_mode` at three seeds on `deep_starmap`** | T09 | **`lookup` beats `medcpt`, established.** Seeds 2/3/4, post-`section_seed`, envelope measured in its own setting: `morans_pearson` **−0.1211 vs an envelope of 0.0246 = 4.9x**, `gearys_pearson` **−0.1129 vs 0.0501 = 2.3x**, signs agreeing on every seed, `morans`'s fold spread 0.0169 (margin 7.2x it). **Second claim in the project to satisfy `claim_min_seeds` = 3 on real data**, and the first about the method's headline novelty. **Stronger than the cautious estimate**: tier-1's `morans` envelope (0.0574) does *not* transfer — deep's own is 0.0246 — so borrowing the denominator would have understated a negative about our own method. `marker_depth_r` is **settled as noise**: −0.0205 / −0.0106 / **+0.0139**, signs disagreeing, 0.1x its envelope — the metric this whole line began from ("`medcpt` beats `lookup` by 5.5x"), which survived a coordinate-frame defect, a layout leak and a fold-balance defect before three seeds ended it. Standing position: **the text channel has no positive on real data at any dataset, and two established negatives on the wide panel.** 6 cold fits at 3.3–3.8 h. `reports/t09_tenv_deep.md` |
| **`marker_depth_r`'s ceiling, and what `cross-mix` is** | T09 | **`cross-mix` under `resample` tracks the nearest-section copy**, identified model-free: its `marker_depth_r` matches a "copy that donor section" measurement to **−0.0009 / +0.0099** on `deep_starmap` and **−0.0503 / −0.0258** on tier-1 (looser — tier-1's profile is far noisier, R 0.87 vs 0.99, and `cross-mix` mixes several donors rather than copying one section). Its scores are a property of the tissue, not of a method. ⚠️ **Headroom over the best copy splits the two datasets and R13's original "the task is saturated" over-generalised from one of them.** `deep_starmap`: split-half R 0.987–0.992, √R ≈ 0.994, best copy 0.929–0.986, headroom **+0.010…+0.064 (median 0.5x the 0.0335 envelope)** — *saturated*; copying is at **98%** of the ceiling and `zinb-flow` at 32%, so its 6.0x+ margins measure copy-vs-not-copy on a task with no room. Tier-1 (28 genes, ~4.1k cells/section): R 0.859–0.882, √R 0.927–0.939, best copy 0.713–0.784, headroom **+0.155…+0.214 (median 5.2x)** — *real room*, copying at **81%** of the ceiling, `zinb-flow` at **67%**, i.e. **83% of copying** against 33% on the dense panel. **Where the task has room the generative path still loses**, and the arms are closest where the task is hardest — which makes tier-1, not `deep_starmap`, the informative reconstruction benchmark. `reports/t09_depth_ceiling_{starmap,deep}.md` |
| **`expr_mode` / `text_emb_mode` on `deep_starmap`** (tier 2, 1017 genes) | T09 audit | **Copying wins everything live; the text channel has no positive.** Re-measured 2026-08-27 after **two defects** were fixed — a coordinate-frame error that scored generated sections in the plane-local `(u,v)` frame (floored `marker_field_r`, `marker_depth_r`, `celltype_localization`) and a leak where `resample` copied the **fold's own** coordinates and cell types. `cross-mix` over `zinb-flow`, margin / worst within-arm fold spread: morans **0.3930 / 6.0x**, gearys **0.5502 / 6.0x**, `marker_field_r` **0.6614 / 6.0x**, `marker_depth_r` **0.5948 / 6.3x**, umap_mixing 0.2364 / ⚠1.5x. `medcpt` vs `lookup`: every sign favours `lookup` and only **`morans_pearson` (−0.1252, 10.0x the fold spread, 3.7x the envelope)** clears both checks — `marker_depth_r`, which the pre-fix run reported as `medcpt`'s 5.5x win, is **−0.0080 (0.2x)**. **Cross-validated**: `cross-mix`'s `marker_depth_r` equals a model-free 'copy the nearest other section' measurement to **0.001 / 0.010** per fold (`reports/t09_depth_ceiling_deep.md`), so `cross-mix` under `resample` *is* a copy and its scores are not a model result. `zinb-flow` reaches **35–36% of copying**, both folds, well clear of the shuffled floor. `celltype_localization` is **structurally inert** for any expression-side gate under `resample` (types come from the copied layout) — a five-metric table, not six. One seed, 2 folds, T08 kernels on internal LOSO, **not** `bench3.evaluate_paper`. ⚠️ **Supersedes the 2026-08-26 row entirely** — that row's `marker_depth_r` findings in both gates were artifacts. `reports/t09_audit_deep_*.md` |
| **`text_emb_mode` on real data (tier-1 STARmap)** | T09 audit | **`lookup` beats `medcpt`** — `morans_pearson` 0.7943 vs 0.7510 (margin **0.0433, 1.3x** the 0.0335 envelope) and `gearys_pearson` 0.7944 vs 0.7519 (**0.0425, 1.3x**); the other four inside the envelope, `celltype_localization` **inert** (constant under `layout_mode=resample`). Folds agree in sign. **The first real-data measurement of this gate at all** — every earlier STARmap number was `lookup` under another name, because all five embedding builders passed zeros or a bare `"Slc17a7."`. 2400 steps, seed 1, **2 folds** (`section_3`, `section_5`), measured under `expr_mode=zinb-flow` because the gate is *inert* under the `cross-mix` the selector shipped. Instrument: **T08 kernels on internal LOSO over training sections** (`specs/09` §10), **not** `bench3.evaluate_paper` — do not place beside the pilot's numbers. `reports/t09_audit_text_emb_mode.md`  ⚠️ **2026-08-27: not re-measured after the coordinate-frame and layout-leak fixes.** Its `marker_field_r`, `marker_depth_r` and `celltype_localization` figures are invalid (the first two were floored by the frame defect, the third by both); `morans_pearson`, `gearys_pearson` and `umap_mixing` are unaffected and stand. On `deep_starmap` the fixes left the `lookup` verdict intact and widened it. |
| **`expr_mode` on real data (tier-1 STARmap)** | T09 audit | **`cross-mix` beats `zinb-flow` on 3 of 6, ties on 3** — morans **0.9255 vs 0.7510** (0.1745, **5.2x**), gearys **0.9293 vs 0.7519** (0.1774, **5.3x**), umap_mixing **0.8098 vs 0.6564** (0.1534, **4.6x**); `marker_field_r` 0.0057, `marker_depth_r` 0.0002 and `celltype_localization` 0.0000 all inside the envelope. **The three it wins are the three T06/T09 already predicted a copy would win** — per-gene autocorrelation and the shared-embedding mixing score are where emitting *real donor counts* is structurally strongest. On the three that measure spatial **arrangement** the two are indistinguishable, and on `marker_field_r` `zinb-flow` is nominally ahead. So: copying wins on count realism by 4.6–5.3x and buys nothing in arrangement. Same instrument and caveats as the row above. `reports/t09_audit_expr_mode.md`  ⚠️ **2026-08-27: not re-measured after the coordinate-frame and layout-leak fixes.** The conclusion "copying buys nothing in arrangement" is **wrong** — on `deep_starmap`, once both defects were fixed, the two arrangement metrics became copying's *largest* margins (0.66 and 0.59). The three distribution-level figures stand; the three arrangement ones do not. |
| Fitted `ell = (ℓx, ℓy, ℓz)` | T03 / T09 | **T09, calibrated on the default fixture, live arm** (`prior_mode="correlated"` + `expr_mode="zinb-flow"`; the selected `iid` + `cross-mix` makes the objective constant and the calibrators now refuse it)**:** `ell_xy` **86.4 µm, converged** in 2 iterations (I_gen 0.4051 vs a flanking 0.4102, inside the 0.02 tolerance) and **applied** to the config by `apply_lengthscale`; `ell_z` **≥ 364.6 µm**, `target_unreachable` — the objective is monotone increasing and runs to the bracket's *top* still undershooting (0.8706 vs an observed 0.9182), so it is a **lower** bound on the tissue and is **dropped**, leaving `Config.ell_z` at 100 µm (C30). Anisotropy `ell_z/ell_xy` **≥ 4.2** vs the fixture's generative 1.7 and the variogram's 2.7. On the dead `cross-mix` arm the same calibration returned 7.0 / 25.0 µm, both `target_unreachable` in 0 iterations, at the bracket's *lower* endpoints — a tie-break on an objective identical to 10 decimal places (0.6727617248) across `ell_z` ∈ {25, 60, 137, 200, 275, 364.6} µm. Control sweeps: the GRF's own between-plane correlation at 100 µm separation rises **−0.005 → 0.921** over that bracket and the full pipeline under `zinb-flow` rises **0.887 → 0.932**, so neither the field nor the 400 µm stack ever limited it (R8). T03 fits: gate fixture (3000 µm): **(102.9, 102.9, 561.1) µm** vs ground truth (120, 120, 200), i.e. ℓxy −14 %; 1000 µm fixture: (141.5, 141.5, 353) µm, +18 %. `ell_z` is extrapolated on both (the 400 µm stack reaches 35 % / 60 % of the fitted sill) and warns |
| `I_gen(ell)` maximiser (bounds T09's calibration bracket) | T03 / T09 | **0.086 × in-plane extent** at 3000 µm FOV (2.52× the fitted `ell`), **0.112 ×** at 1000 µm (0.79× the fitted `ell`) |
| GRF query throughput | T03 | **2.9 × 10⁵ points/s** (10⁶ points, M = 4096, d_h = 64) on the reference 4-core Xeon @ 2.10 GHz — 2.0–3.5 × 10⁵ across runs, the spread being machine load; 8× the points cost 6.6–9.7× the time (ideal 8, quadratic 64) |
| Oblique parity ratio | T04 | **0.955** — G2.1a, *depth-matched on both arms*, and that qualifier belongs with the number. Corroborated by **0.979** on the independent interior-only construction (G2.1b). Fixed-denominator R² by angle 0.4536 / 0.4152 / 0.4018 / 0.4125 / 0.4219 / 0.4386 at 0/15/30/45/60/90° against a nine-arm coronal mean of 0.4208; equal `n` = 1011 (seed 20260815), own source section excluded at every angle, gate fixture 3000 µm FOV. Two superseded constructions kept on the record and **not** to be quoted: 0.941 (per-set denominator, not comparable across angles) and 0.886 (fixed denominator vs a single central coronal plane, which failed) |
| Attention entropy ÷ log K | T04 / T06 | T04: **0.987** (3.422 nats, K = 32) — G2.4 passed at the *opposite* extreme from collapse, near-uniform averaging. **T06: it falls.** With the flow-matching head trained the trajectory is 0.9879 → 0.8563 (minimum 0.8485), a fall of **0.132 log K** against the required 0.05, staying well above the 0.5 log K collapse line. Trajectory in `reports/benchmark.md` |
| Fitted repulsion `r0`, `R`, `gamma`; Potts `beta` | T05 | — |
| Detection-rate r; gene–gene covariance vs independent-donor; mean–variance slope | T06 | detection **r = 0.9955**, MAD **0.0191**; covariance magnitude retained — model **3.3 %** error vs the independent-donor baseline's **7.3 %** (ratio **0.458**, i.e. better by **2.2×**) at equal pattern fidelity (0.9649 vs 0.9750); mean–variance log-log slope **1.7556** vs real **1.7410** (**0.84 %**). Raw Frobenius, which the spec's own criterion is stated on: model 9.316, baseline 7.783, **ceiling 5.601** — and 50 % of 7.783 is **3.892**, i.e. **1.7 below the ceiling**, so the criterion is unsatisfiable by any generator. **The 2.2× does not survive out of sample:** at `consecutive-3` the same decomposition gives 0.995 (no advantage), and it was chosen after seeing which component passed — see B16 |
| Chimerism, isolated (donors fixed, draw varied) | T06 | retained covariance magnitude at 1 / 2 / 3 / 10 mixed donors: **0.978 / 0.920 / 0.897 / 0.844** on the default holdout and **0.955 / 0.818 / 0.783 / 0.714** at `consecutive-3`. Monotone; the competing method's `D = 3` costs 8 pp at 50 µm and **17 pp at 100 µm** |
| Consistency/reconstruction loss ratio; collapse-alarm history | T07 | ratio **median 0.078, max 0.75** (one warning) at the shipped weights over 500 fixture steps; **4.3 median before** `L_thick` was normalised per cell, which is why it is. Collapse alarms: **0** before the ramp, **10** on the shipped-default arm, **35** on the `w_cross = 0.3` arm that collapses (0.065). SEFL overhead **+39 %** per epoch at the configured rates (34-47 % across runs) (+62 % on the full gene panel, which is why `sefl_genes_per_step` exists) |
| Metric-aware on/off table (ablation A2) | T08 | Held-in LOSO reconstruction and the held-out section, alternating holdout, `scripts/t08_metric_report.py`. **off@1200 / on@1200 / off@2400 / on@2400** — reconstruction **1.5901 / 1.6843 / 1.5703 / 1.5885** nats/pair; gene-gene Frobenius **9.000 / 11.154 / 9.049 / 8.489** (baseline 7.70-8.00, ceiling 5.601); Moran's MAE **0.0287 / 0.0408 / 0.0339 / 0.0279**; marker-depth r **0.978 / 0.967 / 0.983 / 0.990**; cell-type localization **0.967 / 0.962 / 0.958 / 0.957**; detection MAD **0.0185 / 0.0314 / 0.0218 / 0.0227**; mean-variance slope **1.762 / 1.734 / 1.773 / 1.722** (real 1.741). Schedule-only control at 1200 steps (LOSO hides a section, no terms charged): Moran's MAE 0.0340, depth r 0.9719, localization 0.9696 — between the two arms, so the cost is the terms' |
| Zero-shot decoding of never-trained genes | T06 / T10 E1 | **r = −0.368** for 40 held-out genes against **+0.946** for the seen ones, on the synthetic fixture. Not noise — negative, and predictable from T02's text/co-expression Spearman of +0.0055: arbitrary gene names carry no MedCPT signal and a held-out gene's `r_g` is exactly 0. The real number needs `resources/gene_meta.parquet` (C14) and is T10's E1 |
| Selected per-dataset config | T09 | Synthetic fixture, `alternating` holdout, 3 interior LOSO folds, 19 fits: **`train_steps = 2400`**, **`w_autocorr = w_profile = w_distribution = 0.5`** (the joint gate — the weights rank 3.5 vs 3.0 at 1200 steps and **1.0 vs 2.0** at 2400), `layout_mode = field`, `prior_mode = iid`, `expr_mode = cross-mix`, `text_emb_mode = medcpt`; config hash `fe49ea9f8ad54bb2`, persisted at `reports/config_selection_synthetic.yaml`. Joint-gate metrics (morans / gearys / mixing / marker-field / marker-depth / localization): **1x off** 0.9197 / 0.8710 / 0.8501 / −0.0527 / 0.0382 / −0.0595; **1x on** 0.8813 / 0.8589 / 0.6560 / −0.0387 / 0.0599 / −0.0577; **2x off** 0.9316 / 0.9022 / 0.9145 / −0.0519 / 0.0195 / −0.0502; **2x on** 0.9368 / 0.9123 / 0.9163 / −0.0370 / 0.0554 / −0.0410. Definition of done **half met**: 5 of 6 against `resample`, **2 of 6** against the independent-donor baseline |
| Headline median gaps; V1 cycle degradation | T10 | **Pilot only — not a headline number, and not quotable as v25's performance.** Real STARmap, tier-1 `paper_2_4_6`, pinned evaluator (`evaluate_paper.py` @ `7362669200bb…8992`), medians over the three held-out sections (`specs/10` §4.6), **one seed** so anything under R10's **0.0335** is not a difference. `decoder_mu_link=exp`, 2400 steps, ground-truth-matched density, shipped `layout_mode=hybrid`: localization **0.6572**, marker-field **0.6384**, marker-depth **0.7478**, Moran's **0.5749**, Geary's **0.5716**, gene-mean Spearman **0.9830**, count ratio **0.894**. Model-free referents beside them — `flanking_copy` 0.7765 / 0.8857 / 0.9794 / 0.9836 / 0.9840 / 0.9863 and `oracle` 0.9808 / 0.9997 / 1.0 / 1.0 / 1.0 / 1.0. **Still ablation A3**: `text_emb_mode=lookup` (MedCPT 403'd in the container), `expr_pca_dim` forced to 16 by the 28-gene panel, and T09 selection and calibration have never run on this dataset. Two things the table establishes rather than estimates: **`gene_mean_spearman` is 0.0033 off its copy floor, inside the envelope** — per-gene magnitude is solved — while **`marker_field_r` is 0.247 below its floor, 7.4x the envelope** and the worst of the six, its fourth appearance as this project's standing weakness (v20 and v21's pooled loss vs SpatialZ, v25's single loss at T09, 0.1611 in the smoke run). The residual is **spatial arrangement, not expression magnitude** — R11 and R12. Superseding the smoke table: Moran's and Geary's were **−0.1999 / −0.2054**, below the `random` probe, before the `decoder_mu_link` fix. Full tables `reports/pilot.md` §13 and `reports/t10_rescore_exp.md`. ⚠️ **The two layout-dependent numbers here — localization 0.6572 and count ratio 0.894 — were measured on the biased rejection sampler and are superseded** by `reports/r11_starmap_layout_modes.md` (grid sampler, one refit: `hybrid` localization **0.6692**, count ratio **5.362**); the other four are unaffected by the layout. V1 cycle degradation: not run |
