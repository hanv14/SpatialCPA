# Coverage matrix — design → specs

Every component of `v23_design.md` and `v23_sectioning_equivariance.md` (SEFL), mapped to the task
file that implements it. **If you find something in either design doc that is not in this table, it
is an omission — flag it rather than skipping it.**

## From `v23_design.md` (base design, = v25 core)

| Design component | Spec | Notes |
|---|---|---|
| Observation token: gene emb + context emb + Fourier(xyz) + expression | T02, T04, T06 | assembled in `spatialcpav25_gen.py` |
| MedCPT gene/celltype/region descriptors + cache | T02 | frozen encoder |
| Free residual `r_g` + gamma anneal + text→residual distillation | T02 | zeros init, anneal order matters |
| Anisotropic Fourier encoding (B_xy=8, B_z=2) | T04 | axis-order bug flagged |
| Triplane anatomical field + TV_z | T04 | |
| Retrieval cross-attention, density-adaptive niche | T04 | |
| **z-proximity term in retrieval** (the competing method's omission) | T04 | `retrieval_w_z`, ablation A5 |
| Gap-aware section dropout curriculum | T04 | `section_dropout_p` |
| Layout: per-type intensity field, Poisson NLL | T05 | the fitted intensity's acceptance number is measured with a **reduced spatial basis**; at the default `fourier_bands_xy = 8` the Poisson MLE overfits the point pattern and T05 specifies no regulariser — open item owed to **T06** (B10) |
| Hard-core radius `r0` at the **1st** percentile (5th selectable, recorded) | T05 | B6; `Config.repulsion_r0_percentile` |
| Strauss/hard-core repulsion fitted to `g(r)` | T05 | ablation A4; the `g(r)` comparison runs over **`[0, 3R]`**, amended at T05 — over the spec's original `[r0, 3R]` a pure-Poisson layout is indistinguishable from the full model (0.070 against 0.093) because the correlation hole ends at `r0` by construction, so A4 would report a false null (B12; `specs/10` §4 amended too) |
| Potts mark smoothing, `beta` fitted not set | T05 | update rule is **Gibbs**, not the spec's ICM, which erases a 2% cell type at any coupling; ICM kept as the asserted negative control (B11). Cell-type localization is reported against **two** references (held-out self-score and flanking section) and the criterion is proposed but **undecided** (B15) |
| `layout_mode` gate (field/hybrid/resample) | T05, T01 | `resample` = previous version |
| Conditional flow matching in cell latent | T06 | straight-line path, Heun; `h1` detached in the CFM loss, `cfm_sigma_min` as the conditional path's width |
| Gene-conditioned ZINB decoder (open-vocabulary) | T06 | bilinear `h ⊙ A e_g`; the **encoder** is open-vocabulary too (a set encoder over `(expression, e_g)` pairs — T06 §2's fixed-width `Enc` would tie the latent to one panel). `Config.decoder_mu_link` keeps the spec's softplus as the default with `exp` selectable and measured |
| Shared latent → gene–gene covariance preserved | T06 | tested vs. the independent-donor baseline — **criterion amended at T06 (B16)**: the spec's "Frobenius error < 50 % of the baseline's" is **below the achievable ceiling** (5.60 measured against a baseline at 7.78, so it asks for < 3.89) and is unpassable by any model. Replaced by (a) the chimerism mechanism isolated with the donors held fixed — which **confirms** the argument, 22 % of the covariance magnitude lost at `D = 3`, monotone through `D = 10` — (b) every arm reported against the measured ceiling, (c) the original criterion held as a strict xfail at 1.20. Retained-magnitude ratio **0.458** (better by 2.2×) at equal pattern fidelity on the default holdout — but **0.995 at `consecutive-3`**, and the decomposition was chosen after seeing which component passed, so the *model-versus-baseline* claim is **not established** by T06. The mechanism (a) and the unsatisfiability (b) are |
| Sample counts, never emit `mu` | T06 | `assert_detection_rate` in the generation path, band `Config.detection_rate_tol`. Detection MAD is **gap-dependent** (0.019 at 50 µm, 0.056 at 100 µm — B17); the acceptance test runs on the default `alternating` holdout |
| Gene subsampling `genes_per_step` | T06 | what makes panel width irrelevant |
| **Trainer**: AdamW, cosine schedule, gradient clipping, EMA teacher | T06 §5 | `train_ctfflow`; `Config.grad_clip`, `lr_min_frac`, `weight_decay`, `log_every`. `forward_train` returns unweighted named terms plus `diag_`-prefixed diagnostics the trainer never weights |
| **Layout intensity basis tied to the fitted length-scale** | T06 (owed by T05, B10) | `fourier_bands_for_lengthscale`; `Config.intensity_basis_ell_multiple`. Recovered r at 300/1200 steps: derived basis 0.979/0.861 against the default 8 bands' 0.835/0.527, i.e. a decay 2.6× smaller. Does **not** abolish the drift — see open risk R4 |
| **Expression head overfits the likelihood** | T06 measurement, owed to T08/T09; **T09 took the enable decision** (joint gate: 2400 steps, weights 0.5); **T08 did not close it** (covariance 11.02 vs a 7.73 baseline, 13.39 vs 11.38 — the claim is downgraded in `specs/10` §2). The enable decision is now **T09 §3's joint selection gate** (`train_steps` × metric weights, on internal LOSO), reported by T10's A2 | open risk **R4**: 1200 → 2400 steps lowers the NLL and worsens every distributional statistic of the generated section. The terms that would stop it are T08's (`w_autocorr`, `w_profile`, `w_distribution`) and T09 §2's calibrators. **`TRAIN_STEPS = 1200` is itself the symptom**, so T09 selects the budget rather than inheriting it |
| Metric-aware LOSO losses (Moran, Geary, profiles, Sinkhorn) | T08 | leakage enforced by type; **principal axis on `TrainingVolume`**, not `Volume` (C10). Built and measured; **all three weights start at 0** because at T06's 1200-step budget the terms cost on every metric they are made of (Moran's MAE 0.0287 -> 0.0408, depth r 0.978 -> 0.967, Frobenius 9.00 -> 11.15) — but the ordering **reverses at 2400 steps** on four of six statistics. A 0 calibrated at 1200 steps is calibrated to an undertrained model, so it is **not hardcoded**: the weights and `train_steps` become **one joint selection gate** at `specs/09` §3, chosen per dataset on internal LOSO, and **A2 is an addition experiment run at two budgets** (`specs/10` §4). `specs/08`'s comparison of a model against a measurement is under-specified and all three naive readings diverge (C24). `loss_profile` gains keyword-only cell types (C21); the divergence is the in-repo Sinkhorn, never `geomloss` (C20) |
| Uncertainty-gated anchoring (replaces alpha/gap flags) | T09 | isotonic `w(v)` — **built** as `Config.expr_mode="auto-blend"`: `M = n_uncertainty_samples` flow samples under *different GRF realisations* give the per-cell latent variance, PAVA fits a **non-increasing** `w(v)` on LOSO folds, and the mix is the v20 Bernoulli cross-mix per gene so the output stays counts. No `gap_scale` / `alpha_tol` / `edit_weight` exists anywhere in the package, and `auto-blend` without a fitted `w(v)` raises rather than defaulting to one. Fitted against a **per-cell profile correlation**, not an element-wise error: the expected element-wise error of a Bernoulli mixture is linear in the weight, so its minimiser is always 0 or 1 |
| Leakage-free length-scale + detection calibration | T09 | flanking sections only; **unimodal objective** — bracket capped, maximum located, `target_unreachable` status (T03/GATE 1); one **global** `ell`, per-module agreement a diagnostic only (A2). **Built, measured, and now written back** — `apply_lengthscale` (C30, `specs/09` §2 amended) is the only route from a calibration to the prior and applies **only a `converged` axis**, dropping the rest with a warning naming achieved and target. Both calibrators also refuse configurations where `ell` cannot reach the output at all (`prior_mode="iid"`, `expr_mode="cross-mix"` — the latter makes the objective constant to 10 dp across a 15× sweep). On the live arm `ell_xy` **converges to 86.4 µm** and is applied; `ell_z` follows R1's remedy 2 and runs to the bracket's top still undershooting, so it is a **lower bound** (`ell_z ≥ 364.6 µm`) and is dropped — remedy 3's guard intact. The detection half has **no headroom on the fixture** — model error 0.0217 against a real between-section variation of 0.0397 — so it is fitted, reported and **not applied by default** (C28) |
| **Mean–variance calibration** (`log theta` per gene, beside `pi`) | T09 §2 | design §3.5; `DetectionCalibration` carries both (D-table). Both intercepts are **solved exactly** by bisection on a closed form, **alternately** — the two are not orthogonal (`theta` enters detection through `P_NB(0)`, and solving them independently made the detection MAE 0.055 → 0.230), and the `theta` target is the **mean–variance relation** at the model's own mean rather than the absolute variance (C28) |
| **v20 Bernoulli cross-mix** (`expr_mode="cross-mix"`) | T06 §4b | design §5/§6; behaviour pinned by `test_cross_mix_matches_v20` (A6) |
| Automatic per-dataset config selection | T09 | coordinate descent, ~10 fits — **plus one joint gate that is not coordinate-descended**: `train_steps` × the metric-aware weights, all four cells of `{1x, 2x} x {off, spec weights}` scored together, each at its own budget (added at T08; visiting them separately would pick "weights off" from a 1200-step incumbent and never reach the cell that wins). `Config.train_steps` is the persisted budget. **Run on the fixture (19 fits, 9195 s): the gate chose `(2x, weights on)`** — rank 1.0 against 2.0/3.0/3.5 — so `train_steps = 2400` and all three weights ship at **0.5** for this dataset, and T08's interaction reproduced exactly (the weights *lose* at 1x). The selected config is persisted as YAML beside the report. **And a second gate that is not coordinate-descended either, added after R8**: `layout_mode` x `prior_mode` x `expr_mode` are merged into one **18-cell gate at the selected budget**, because `specs/09` §3's **training-free-option rule** disqualifies all three from reduced-budget scoring — each has an option that reaches final behaviour without training. The rule is enforced by `TRAINING_FREE_OPTIONS` and an import-time check, so a future gate must be classified when it is added. Re-run on the fixture (23 fits, 21 330 s): all six `cross-mix` cells rank **bottom**, all three gates changed answer, and the selection is `resample` + `correlated` + `zinb-flow` + `lookup` (`00ef4a19a2f576b8`). The rule was then **widened (R9)**: a gate is also escalated when the incumbent is *unconverged* at the reduced budget, measured per run by `incumbent_is_unconverged` — it fires here (shortfalls 0.36 / 0.43 / 0.81 against a 0.05 tolerance) and re-scoring `text_emb_mode` at 2400 keeps `lookup` (rank 1.2 vs 1.8). **But the margin is inside the reproducibility envelope**: the same config refitted at the same seed drifts up to 0.0120 against a 0.0110 option gap, so the gate is undecidable at one seed (new risk **R10**). Checkpointed per cell (`ScoreCache`) so a multi-hour selection resumes rather than restarts |
| No-regression guarantee | T09, T01 | `test_selector_can_recover_v20_config` — `resample` + `cross-mix` is on the gate grid and is selected when the new components are degraded |
| Six target metrics, **vendored verbatim from `bench3/evaluate_paper.py`** | T10 | content hash pinned, bitwise agreement asserted; v20's two bugs are a footnote about v20's own tuning signal, not a fix to the benchmark (A3). **Each metric additionally needs its achievable ceiling measured** on the synthetic fixture from an independent draw of the known generative law, with method / ablation / baseline numbers reported relative to it — added at T05, which measured `celltype_localization`'s ceiling at 0.72 against a self-score of 0.92, and per-type because the metric is unstable for abundant tissue-wide types (B15, B15a) |
| **Competitors vs development history** | T10 §3 | The competitors are the three **published** methods — SpatialZ, FEAST, isoST. Prior SpatialCPA versions (v14–v24) are **internal development history**, reported in a separate development table; **v20 is the no-regression reference** (`test_selector_can_recover_v20_config`) and nothing more. No headline claim is framed around beating an internal version |
| **The estimator is the MEDIAN** | T10 §4.6 | Stated once, used everywhere: median over sections with a 95% bootstrap CI, median across seeds with min-max spread, median difference for every gap. **Never a mean.** The paper design holds out three sections, and at n=3 one outlier inverts a verdict — measured: `marker_depth_r` v20 sections [0.7422, 0.9704, 0.9783] give median **0.9704** (beats SpatialZ's 0.9267) but mean 0.8970 (loses to 0.9200). And the outlier is structural, not noise: **`section_2` is the worst held-out section in 37 of 49 (method, metric) cells**, because it is bracketed by the stack's first section — **R3 appearing inside the tier-1 design**. `assert_no_mean_over_sections` enforces it |
| **Invocation contract: no tuning flags** | T10 §10.1 | v25 runs in v20's command shape (`run_all --methods spatialcpav25_gen --dataset X [--design wide]`), resumable, one command. v20 took **eleven** tuning flags after the `--`; **v25 takes none** — `fit` taking no method flags is a paper claim, so a bare invocation must produce the shipped config, asserted by `test_bare_invocation_reproduces_shipped_config` on `Config.content_hash()`. The only extras are the shared `_v2_io` args and the §6 ablation switches. `--results-root` is **not** a wrapper flag and cannot be: `run_single` resolves `out_dir` before invoking the wrapper, so the root is the `BENCH_V3_RESULTS` env var. ⚠️ **A8 has no `Config` gate** — `loss_prog_WRONG` is built but deliberately outside the weighted loss set, so T10 must add `Config.w_prog_wrong = 0.0` or A8 cannot run as a config override |
| **Per-dataset selection is cached out of band** | T10 §10.1 | Selection is ~23 fits, so running it per invocation would make every bare run cost ~23 fits before a single section — weeks on `allen_merfish_brain`. It runs **once per dataset**, persisted to `$SPATIALCPAV25_SELECT_DIR/<dataset_id>/{selected.yaml, scores.csv, selection_report.md}`, **shared across every seed, arm and tier** (an ablation inherits the shipped config), and deliberately **not** under `BENCH_V3_RESULTS`. ⚠️ **`ScoreCache.key` is `content_hash:steps` and omits the dataset**, so a shared cache file would return one dataset's score for another's cell — the per-dataset directory is a correctness requirement. `selected.yaml` carries a volume fingerprint and a mismatch **raises** rather than re-selecting or reusing. `--select-only` pre-warms; `--require-config` refuses to select, which is what campaign runs use |
| **Named pilot criteria C1 / C2** | T10 §11.1 | **C1** — `paper_celltype_localization` on tier 1, the one metric where the published competitor leads the whole SpatialCPA line (medians: SpatialZ 0.8372 vs best internal 0.8227, v20 0.7760 — and under the median it is SpatialZ's **only** tier-1 win, against every SpatialCPA version) and exactly what T05's intensity-field layout head exists to attack; reported **per cell type as well as pooled**, because the metric's null collapses for abundant tissue-wide types. **C2** — the wide regime, where by median SpatialZ beats v20 on `morans_pearson` in **6 of 7** wide holdouts and `gearys_pearson` in **5 of 7**, so the design docs' "wins decisively at wide gaps" is contradicted rather than merely unsupported (`marker_field_r` goes the other way, 0 of 7). The prior campaign has **no head-to-head wide run on STARmap at all**, so the pilot generates the first one. Neither is pass/fail; a criterion that does not move is a result |
| **Prior-campaign evidence is not a baseline** | T10 §13 | ⚠️ The recovered `per_section_metrics.csv` is **evaluator-heterogeneous**: `field_ssim` / `detection_spearman` / `rare_celltype_*` are on 132/132 rows for v18/v20/v21 but 33/132 for SpatialZ and **0/3 on STARmap** — so the tier-1 head-to-head was scored by **two different `evaluate_paper.py` revisions**, the exact failure A3's SHA pin exists to prevent. Its numbers are **directional signals only**; every one is re-measured on the pinned instrument. `evaluate_all --force` re-scores recovered predictions without re-running any method. v22/v23/v24 were never run |
| **`marker_field_r` watch item** | T10 §13.4 | v25's single loss on the fixture at T09, and where SpatialZ is relatively strongest against the SpatialCPA line. But stated precisely: **on tier 1, v20 (0.8804) and v21 (0.8881) BEAT SpatialZ (0.8522)**, per dataset it is **9-9**, and in the wide regime v20 wins **7 of 7**; only the forbidden cross-dataset pool favours SpatialZ. A watch item, not a defect. Diagnostic: it is one of only two **pose-dependent** metrics, so report `align_rotation_deg` / `align_score` / `align_runner_up` beside it — tier-1 rotations are 0.0–1.5 degrees, so alignment is **not** confounding that ordering, and a v25 regression at 0 degrees points at T05's intensity head, not `align.py` |
| **Two-tier reporting discipline** | T10 §1 | **Tier 1 = STARmap under `paper_2_4_6`, unmodified** — the protocol-faithful headline and the direct SpatialZ comparison; nothing may be varied. **Tier 2 = anything needing a modified design** — boundary rows, re-sectioning, the wide regimes, the analogue datasets. `assert_tier_purity` refuses a tier-1 table containing any other row; tiers are never merged, averaged or ranked together |
| **Additivity contract** | T10 §0 | Exactly **two** bench3 files are touched: a new `methods/run_spatialcpav25_gen.py` and **one appended `METHODS` dict entry**. Not `METHOD_ORDER` (so a bare `run_all` plans the previous campaign), not `DATASET_SPECS` (derived datasets are passed **by path**), not `design.py` (the boundary holdout is a dict handed to `run_single`), and **never `evaluate_paper.py`** — its SHA-256 is asserted before *and* after every campaign run. Existing results never need re-running |
| **Statistics** — Wilcoxon + BH, bootstrap CI, Cliff's delta, forest plot | T10 §4.5 | **bench3 has none of it**; `rank_methods` averages group ranks, which is not significance. Reads `all_metrics.csv` / `per_section_metrics.csv` from outside. ⚠️ **Never `summary_by_method.csv`** — `aggregate_results.summarize` means every metric **across holdout ids**, so a mixed root would average `paper_2_4_6` with `wide_3_4_5`, which is exactly what the Do-NOT forbids |
| **Repeated-seed rule** (`CLAIM_BEARING`, `_check_claim_coverage`) | T10 §4.2 | `Config.claim_min_seeds = 3` on claim-bearing measurements only; **bench3's results path has no seed dimension**, so each seed gets its own `BENCH_V3_RESULTS` root — no path or schema change. Report min–max spread beside every claim-bearing median; a claim smaller than the campaign envelope is a tie (R10) |
| **Boundary design for R3** | T10 §4.3 | bench3 cannot measure it: `paper_design` holds out 2/4/6 and `loo_design(exclude_boundary=True)` is hardwired, so **no exposed design ever holds out section 1 or 7**. `run_single` takes the holdout config as a plain dict, so the driver constructs `boundary_1` / `boundary_7` itself — `design.py` untouched. A boundary section is **extrapolation, not interpolation**: Tier 2, its own table, the two ends reported separately |
| **Re-sectioning preprocessor** | T10 §9 | STARmap is a real 3-D point cloud, so re-slicing gives genuine orthogonal sections with **real ground truth** — the oblique claim validated on real data. Emits a **new dataset id**, passed by path; source asserted bitwise unchanged. ⚠️ **Every bench3 volume is a thin slab**: STARmap is ~1545 x 1545 x 77 um, so a 90-degree re-slice gives 220 um sections (20x the original thickness) with a 20:1 footprint that degenerates `FIELD_GRID`'s 20x20 binning to 1-D. So E3 **sweeps the angle**, `resection` refuses a build past `Config.resection_max_aspect`, and the laminar-gradient axis is measured **before** the normal is chosen |
| **Regimes** `alternating` / `consecutive-3` / `consecutive-5` | T10 §4.4 | **All three already exist**: `paper_2_4_6` *is* the alternating design (bench3 generalises it as "hold out every even section"), and `design.py::consecutive_design` gives the wide ones (`--design wide [--holdout-block 5]`). ⚠️ `consecutive-5` on STARmap clamps to n-2, leaving **only sections 1 and 7** (~8.2k training cells) — the thin-evidence extreme, not a peer regime |
| **Cell-type-free datasets out of scope** | T10 §5.3 | `openst_lymph_node` and `visium_mouse_brain_c2l` are 100% unannotated. bench3 writes a single `"unknown"` category so `data/schema.py` (which **requires** `Section.cell_type`) loads them, but Potts smoothing is degenerate (purity identically 1.0) and the whole `localization` rank group is lost, making the composite a 4-group score incomparable with every other dataset's 5-group one. A type-free layout path is **T05 work** and must exist before either is run |
| Unoptimised control metrics | T10 | paper-integrity requirement. **Five of the six are already bench3 columns** (`gen_sinkhorn`, `gen_coexpression_agreement`, `gen_celltype_nhood_agreement`, `paper_gene_var_spearman`, `paper_gene_detection_spearman`); T10 builds **only `duplicate_profile_rate`**, which is also what the above-ceiling check reads |
| Baselines incl. SpatialZ at published defaults | T10 | deep-copy guard; **v14/v18 dropped explicitly** with the reason in the methods (D-table). SpatialZ, FEAST, isoST and v20 are **run from bench3's own `METHODS`**, not wrapped; `run_nearest_copy` is bench3's `flanking_copy` probe; only `run_convex_interp` is new. ⚠️ **The existing v20/v22 numbers are NOT in this repo** — every `results/` tree is absent — so the comparators must be re-run for comparability (`specs/10` §3) |
| **Dataset requirement**: >= 1 non-brain, >= 1 non-transcriptomic panel | T10 §5.2 | design §7; refuses a headline table without both (D-table). ⚠️ **The two halves collide**: `imc_breast_cancer` satisfies both alone but is `fluorescence_intensity` -> `Config.decoder="zigamma"`, which has **never been trained, generated from or calibrated**, and T09's detection / mean-variance calibration is **ZINB-only** (`_zinb_detection`, `calibrate.py:1086`). So the non-brain half is discharged claim-bearingly by `exseq_breast_cancer` or `cosmx_nsclc_3d` (both `raw_counts`) and the IMC row is a **labelled non-claim-bearing diagnostic**. Validating ZIGamma is T06/T09 work, not T10's |
| Ablations A1–A8 | T10 §6 | gates in `Config`, wired as wrapper flags; **each arm gets its own `BENCH_V3_RESULTS` root**, so no bench3 path or schema changes. Arms **inherit the headline dataset's selected config** and override one gate — an arm never re-runs config selection |
| Capability experiments E1–E5 | T10 §7 | zero-shot, cross-panel, oblique, throughput, intersection. **E1 reports both distillation arms** (`r_g = 0` and `r_g = psi(t_g)`) (D-table) **and both summary arms** (`summary_source == "native"` vs the orthologue fallback — T06, SPEC_QUESTIONS B20), **and two gene splits** (markers held out / markers seen), because the marker panel comes from `uns['paper_protocol']` and a split that hides it disables `paper_marker_*`. ⚠️ **E1's home moves from STARmap to `deep_starmap`**: 20% of a 28-gene panel is 5–6 genes (noise, and 3 of the 28 are markers), while `deep_starmap` is 1017 genes, `raw_counts`, and runs `paper_2_4_6` — **the same design as tier 1**. `allen_zhuang_abca2` is ruled out (`log2_normalized` -> zigamma, and `paper_alt7of15`). ⚠️ **Blocker**: `resources/gene_meta.parquet` is exactly zhuang_abca2 (1122) ∪ starmap (28) = 1138 mouse-cased symbols (only 3 uppercase), and `deep_starmap`'s symbols are **uppercase**, so every one misses on exact match — case folding is mandatory and post-folding coverage is unmeasured; a new table needs `mygene` network access recorded as 403'd here (C14). **Gene-subset scoring is free**: `evaluate_paper` scores on `intersect1d(pred.gene_names, gt.var_names)`, so emitting only the held-out genes is the whole mechanism. **E4 is amended: a structure-recovery figure, not a scored row** — there is no ground truth at 10x z-density. **E5 needs no ground truth and no bench3 at all** |

## From `v23_sectioning_equivariance.md` (SEFL)

| SEFL component | Spec | Notes |
|---|---|---|
| **3D GRF noise field** replacing the per-section 2D prior | T03 | GATE 1; RFF, Matérn |
| Anisotropic `ell = (ℓx, ℓy, ℓz)` fitted from sections | T03 | what makes oblique quantitatively right |
| Continuity in 3D → exact intersection consistency | T03 (G1.2), T07 | bitwise-equality test |
| `with_lengthscale` without redrawing | T03 | calibration loop stability |
| **Unimodal `I_gen(ell)`; calibration bracket + maximum detection** | T03 (G1.3g), T09 §2 | measured in `reports/gate1.md`; `calibration_ell_max_extent_frac`, `calibration_ell_max_fitted_multiple` |
| Rotation augmentation over the whole volume | T04 | `RotationContext` |
| Multi-orientation triplane ensemble | T04 | `n_plane_orientations=4` |
| **Oblique parity ≥ 0.90 × axis-aligned** | T04 | GATE 2; evaluation set = pooled cells within `thickness/2`, **equal `n` across angles** and **own source section excluded from retrieval** (C1). Criterion amended at T04 (C16): **both arms depth-matched** — the 0° arm is the mean over coronal planes at every section — plus a required interior-only check, and a **fixed** R² denominator. Measured 0.955 / 0.979 |
| Fourier low-frequency axis follows the data frame | T04 | the silent-bug test |
| Plane geometry, `intersect`, `random_plane_pair` | T07 | hand-computed test cases |
| Curved / anatomy-following surfaces | T07, T09 | `generate_curved` — the layout is sampled on the surface's mean plane and every cell is **lifted onto the surface** before any field, GRF or retrieval query, so the in-plane point process is the one T05 fitted while every expression query happens at the cell's true curved position |
| `L_cross` intersection consistency | T07 | decoder parameters matched directly (L2 on `log mu`, `log theta`, `pi` logit) — **no KL surrogate** (C2). Two of the four terms are computed in log/logit coordinates rather than the spec's raw ones (SPEC_QUESTIONS C17): `lambda` because its squared difference is ~1e-10 at any real intensity, and the type term as `KL(p2 || p1)` — the same gradient as the spec's CE, without the teacher-entropy floor that would make the 60 % fall unreachable. The branches differ by **pose** (`plane_pose`), which is the only channel T04 leaves pose-dependent |
| **EMA teacher + stop-gradient (anti-collapse)** | T07 | disabled-teacher test must fail. A separate module (`EMATeacher`), not T06's `EMA.swap_in`: swapping copies into the *live* parameters, and a parameter mutated between a forward and its backward trips autograd's version counter |
| Collapse alarm on per-gene variance | T07 | |
| `L_thick` coarse-graining; counts add, no per-cell matching | T07 | |
| `L_prog` conditioned on (cell type, region) | T07 | unconditional variant tested as wrong |
| Invariant vs. equivariant table as a code constant | T07 | `INVARIANT_QUANTITIES` |
| `loss_prog_WRONG` negative control | T07 | ablation A8 |
| SEFL warm-up + ramp; consistency must not dominate | T07 | ratio logged, warned |
| SEFL cost control (every-N steps, patches) | T07, T01 | `sefl_*` config. `specs/07`'s own two requirements collide: at the full panel the block costs **+62 %** against its own **< 60 %** cap, so the losses take a separate, smaller gene budget (`sefl_genes_per_step = 64`, **+34 %** measured) — every SEFL term is a mean or a covariance over genes and the subsample is redrawn every step (C18) |
| `Section.thickness` as a first-class field | T01 | assumed-value warning |
| Slab-volume intensity integral (not area) | T05 | makes `L_thick` coherent |
| One GRF realisation per stack → mutual coherence | T09 | `generate_stack/oblique/curved` resolve `grf_seed` **once** per call; reusing it across calls extends the guarantee between them, which is why it is an exposed parameter. `test_stack_coherence` pins the decay and the absence of a spike at a training section's depth; `test_stack_shares_one_realisation` pins that the seed is what carries it |
| E5 intersection agreement vs. the competing method | T10 | the structural-difference figure. **T09 measured the criterion and half of it is above its ceiling** (C27): concordance **0.814** against the spec's 0.8 (ceiling 0.781), expression correlation **0.724** against the spec's 0.85 with a measured ceiling of **0.726** — two independent draws of one plane under one realisation. The headline test asserts concordance absolutely and correlation ceiling-relative; the literal 0.85 is a strict xfail |
| **V1 re-sectioning cycle** | T10 §8 | needs the T10 §9 re-sectioning preprocessor for the sagittal target; the cycle **degradation** is the reported quantity |
| **V2 orthogonal-specimen validation** | T10 §8 | overlaps E3. ⚠️ **No bench3 dataset is a second specimen cut on a different axis**, so V2's literal form has no data. What T10 §9 provides is the *same* specimen re-sectioned (= E3): stronger ground truth (real cells, same animal), weaker generalisation claim (no cross-animal transfer). State the distinction rather than presenting E3 as V2 |
| **V3 anisotropy prediction** | T10 §8 | the equivariant-column payoff; reads T10 §9's angle sweep, so it costs no extra fits |
| **V4 thickness transfer** | T10 §8 | validates `L_thick`. **Primary form is V4a**: `merfish_thick_hypothalamus` re-partitioned into 14 thin slabs vs its shipped 7 thick ones — same volume, same cells, same panel, `zinb` both sides, thickness the only variable. The cross-dataset pair (`merfish_hypothalamus` thin, `normalized` -> `zigamma`) is **V4b, a labelled diagnostic**: a thin-vs-thick transfer measured across a decoder change is confounded |
| Ablation A7 (SEFL off) | T10 | **Inverted at T07: SEFL ships off, so A7 is an *addition* experiment** (`w_thick = w_prog = 0.2` against the shipped default). `w_cross` is 0 because intersection consistency is exact by construction in v25 and training the loss flattens the field (C19, R6); `w_thick` / `w_prog` are 0 because at their spec weights a model trained at T06's own budget fails three T06 acceptance tests (R7). SEFL's net contribution is **unverified** until A7 reports the six target metrics |

## Gates, restated

| Gate | Spec | Criterion | If it fails |
|---|---|---|---|
| GATE 1 | T03 | GRF prior halves Moran's I error vs. i.i.d.; `I_gen` monotone in `ell` **over the calibration bracket** and unimodal with its maximiser at or above the fitted `ell` (G1.3g); measured on the 3000 µm gate fixture | **Stop.** The method's core mechanism does not work; report before building anything else. |
| GATE 2 | T04 | **depth-matched** oblique R² ≥ 0.90 (G2.1a) **and** the interior-only check (G2.1b), on equal-`n` sets with own-section retrieval excluded; plus permanent G2.1h (augmentation complete, by mutation) and G2.1i (draw-noise floor) | Raise orientations to 8, verify augmentation covers everything *by mutation*, check the shortfall exceeds the G2.1i floor, then **stop** — a steerable backbone is a design change. **Ran at T04: 8 orientations bought +0.00086, all channels verified wired.** |

## Deliberate negative controls

Two things are built *to fail*, and their failure is a result reported in the paper:

1. `loss_prog_WRONG` (T07) — constrains in-plane Moran's I across angles, i.e. wrongly treats an
   equivariant quantity as invariant. Trained as ablation A8; expected to be worse.
2. Independent-donor sampler (T06, T10) — `eval/baselines.py`, reimplements the competing method's
   per-gene independent draw. Used as the reference point for the gene–gene covariance claim. It
   shares its mechanism with the v20 cross-mix deliberately: "pick a donor per gene and take its real
   count" is one operation, and the two methods differ in the *weights* (v20 mixes two donors at a
   weight that is zero at narrow gaps; the competing method mixes ≤ 3 uniformly, always).

Neither is dead code. Do not delete them as unused.
