name: bench_spatialcpav3
channels:
  - conda-forge
  - pytorch
  - defaults
dependencies:
  - python=3.10
  - numpy>=1.24
  - scipy>=1.10
  - pandas>=2.0
  - scikit-learn>=1.3
  - h5py>=3.9
  - pytorch>=2.0
  - pip
  - pip:
    - anndata>=0.10
    - scanpy>=1.10
    - tqdm
    # SpatialCPA v3 is imported from the `spatialcpav3/` package at the
    # repository root (one level above benchmark-pbya) — no pip install needed.
    # The wrapper adds it to sys.path automatically; override with
    # SPATIALCPAV3_ROOT if the layout differs.
