# PROGRESS — SpatialCPA-v25-Gen

Status of every task in `specs/`. Update the row when a task lands, then append an entry under
"Log" with what was built, the test numbers, and any deviation from the spec and why.

Status values: `TODO` | `IN PROGRESS` | `BLOCKED` | `DONE`.

| # | Task | Spec | Key deliverables | Gate | Status |
|---|---|---|---|---|---|
| T00 | Project scaffolding | — | `CLAUDE.md`, `PROGRESS.md`, `pyproject.toml`, `Makefile`, ruff/mypy/pytest config, `SPEC_QUESTIONS.md` | — | DONE |
| T01 | Config and data contracts | `specs/01_TASK_config_and_data.md` | `config.py`, `data/schema.py`, `data/loaders.py`, synthetic fixture | — | DONE |
| T02 | Text-grounded embeddings | `specs/02_TASK_text_embeddings.md` | `data/text.py`, `model/embeddings.py`, MedCPT cache, distillation head | — | DONE |
| T03 | 3D GRF noise field | `specs/03_TASK_noise_field_GATE1.md` | `model/noise.py`, `scripts/gate1_report.py`, `reports/gate1.md` | **GATE 1** | DONE — **GATE 1 passes** on the 3000 µm gate fixture |
| T04 | Anatomical field + retrieval | `specs/04_TASK_field_and_retrieval_GATE2.md` | `model/field.py`, `model/retrieval.py`, `scripts/gate2_report.py`, `reports/gate2.md` | **GATE 2** | DONE — **GATE 2 passes**, depth-matched oblique parity **0.955** (edge-excluded check **0.979**); amended 2026-08-16 with the per-query z window (SPEC_QUESTIONS C1c), **no gate criterion changed verdict** — restated 2026-08-25 from the wider "gate numbers unchanged", which was measured on the *evaluation* path (`test_gap_relative_window_is_identity_on_a_regular_stack`, bitwise) and does not cover `G2.1h-c`'s off-stack mutation arm, where the amendment moved the reported value 40.28 -> 63.97 (C32, reproduced exactly). Audit: [progress/t04_field_and_retrieval.md](progress/t04_field_and_retrieval.md) |
| T05 | Layout head | `specs/05_TASK_layout_head.md` | `model/layout.py`, `losses/reconstruction.py` (layout NLL), `infer/planes.py` (minimal `Plane`), intensity + Strauss sampler + Potts marks | — | DONE — all eight acceptance tests pass, both negative controls fail as they must. **Amended 2026-08-24**: the position sampler for `field` / `hybrid` was measurably biased (`reports/r11_envelope.md` defect 3 — a sampled-maximum envelope with an unclamped acceptance ratio, so the draw came from `min(lambda, envelope)`). `reports/r11_fix_options.md`'s **option D** — a grid-multinomial sampler with no envelope, no acceptance ratio and a closed-form expected count per cell — is now the default (`Config.layout_sampler="grid"`); `"rejection"` stays selectable and is asserted *wrong* as the negative control. Validated against a closed-form intensity at max/mean **235** with **no fit and no data** (16 tests). T05's own numbers re-measured: generated/ideal **97.8 %** (was 99.4 % on the biased sampler), generated/flanking 1.491 / 1.514 / 1.059 |
| T06 | Expression head + ZINB decoder | `specs/06_TASK_expression_head.md` | `model/expression.py`, `model/spatialcpav25_gen.py` (`CTFFlow` + trainer), `losses/reconstruction.py`, `eval/baselines.py` | — | DONE — with three recorded failures: the covariance criterion is **unsatisfiable as stated** (below the ceiling, B16) and the model half of the amendment does **not** hold out of sample; zero-shot decoding is **r = −0.368** (B18); T05's intensity overfit is answered at trainer level but not abolished (R4) |
| T07 | SEFL consistency losses | `specs/07_TASK_sefl_losses.md` | `losses/sefl.py`, `infer/planes.py`, EMA teacher, collapse alarm | — | DONE — **SEFL ships opt-in (all three weights 0)**, with one result that reframes the loss it belongs to: intersection consistency is **exact by construction** in v25 (bitwise, untrained), so `L_cross` has only the augmentation pose left to constrain and minimising that flattens the field (generated per-gene variance **0.065** against **0.711** with SEFL off). `w_cross` ships at **0**, the failure is a strict xfail, and the decision is R6 / SPEC_QUESTIONS C19. `L_thick` and `L_prog` land as specified but are **also off**, because at their spec weights they break three of T06's acceptance criteria (R7); A7 at T10 decides whether SEFL is used at all |
| T08 | Metric-aware LOSO losses | `specs/08_TASK_metric_aware_losses.md` | `losses/metric_aware.py`, `train/loso.py`, `scripts/t08_metric_report.py` | — | DONE — with **both** headline criteria red and the decisions they force taken: the terms **ship at 0** (opt-in, A2 becomes an addition experiment) because at T06's 1200-step budget they cost on every metric they are made of, and open risk **R4's covariance criterion is not met on either holdout** (11.02 vs a 7.73 baseline at `alternating`, 13.39 vs 11.38 at `consecutive-3`), so the paper's covariance claim is **downgraded to a mechanism claim** in `specs/10` §2. The finding that qualifies both: at **2400** steps the ordering reverses on four of six statistics — the terms are slower, not worse, and 1200 steps is R4's own symptom |
| T09 | Inference + calibration | `specs/09_TASK_inference_and_calibration.md` | `infer/generate.py`, `infer/calibrate.py`, `train/select.py`, `scripts/t09_report.py`, `reports/config_selection_synthetic.md` | — | DONE — **the joint gate decided what T08 deferred: `train_steps = 2400` with all three metric-aware weights at 0.5** (the weights lose at 1x, rank 3.5 vs 3.0, and win at 2x, 1.0 vs 2.0, on four of six metrics — the cell a one-gate-at-a-time selector cannot reach). Four results the run forced: the reduced-budget scoring first switched `prior_mode` to **iid** and `expr_mode` to **cross-mix**, which R8 then showed to be an artefact of the 25 % budget — after the **training-free-option rule** merged `layout_mode` x `prior_mode` x `expr_mode` into one 18-cell full-budget gate, the selection is **`resample` + `correlated` + `zinb-flow` + `lookup`** (`00ef4a19a2f576b8`) and the definition of done against the independent-donor bar goes **2 of 6 → 4 of 6**; the calibration on the *selected* config was an **artefact of a flat objective** — `cross-mix` never evaluates the flow, so `ell` cannot move a single emitted count — and the calibrators now refuse that path as well as `iid`, while `apply_lengthscale` (C30) writes a **converged** axis through to the prior and drops the rest; on the live arm `ell_xy` converges to **86.4 µm** and the anisotropy is bounded at **ell_z/ell_xy ≥ 4.2**; E5's expression criterion is **above its achievable ceiling** (0.724 measured against a ceiling of 0.726 and a spec threshold of 0.85 — strict xfail); and the definition of done, **on the reselected config**, is met on 4 of 6 against both `resample` and the independent-donor baseline (0.9517 / 0.9142 / 0.9730 on the three distribution-level metrics), with one tie and one loss each. **2026-08-26 — the machinery for the per-dataset run on tier-1 STARmap is built and the campaign is not yet run.** The finding that made it necessary: **`text_emb_mode="medcpt"` had never been exercised on real data and could not have been** — all five embedding builders passed either zeros or `gene_descriptor(symbol, None)`, so the encoder saw the bare string `"Slc17a7."` while `resources/gene_meta.parquet` carried a full name and an NCBI summary for **28/28** of the panel; every STARmap number to date is ablation A3's `lookup` arm under another name. `model.build_entity_embeddings` is now the single builder and both arms of the gate get the same vectors. Four defects that would each have stopped or spoiled the run are fixed: selection through the bench3 wrapper **raised at its first line** (no embeddings factory, so no per-dataset selection had ever run on any dataset); `expr_pca_dim = 32` exceeded the 28-gene panel (**the owed fix, now `clamp_config_to_volume`** — the rule gives 28, where the pilot's 16 was a stand-in); `fit_repulsion` was paid for under `layout_mode=resample`, which never reads it and where it *raises*; and one under-trained candidate aborted the whole search, where `assert_detection_rate`'s band against STARmap's 0.9999 detection makes that the expected case — such a candidate now ranks last, loudly, and a gate that fails entirely is refused. Two mechanisms `specs/09` §3 describes and nothing implemented are now wired: the **capability tie-break** (applied by hand to a printed table at §13, so a persisted `selected.yaml` would have carried the rank winner) and **pinned gates** with a required reason. **2026-08-26 — both real-data audits are in, and both are negative.** On tier-1 STARmap `lookup` beats `medcpt` by **1.3x** the 0.0335 envelope on Moran's and Geary's (the first real measurement of that gate at all), and `cross-mix` beats `zinb-flow` by **4.6–5.3x** on Moran's, Geary's and `umap_mixing` while tying inside the envelope on all three arrangement metrics — i.e. **copying wins exactly the three metrics T06/T09 predicted a copy would win** (real donor counts maximise per-gene autocorrelation and shared-embedding mixing) and buys nothing where a generative advantage would show. One seed, **2 folds**, T08 kernels on internal LOSO — not `bench3.evaluate_paper`. **2026-08-27 — two defects invalidated the arrangement half of every internal-LOSO number, and fixing them inverted the reading.** (1) `section_scores` was handed the *plane-local* `(u,v)` coordinates a generated `AnnData` carries at `obsm[coord_key]` instead of the physical `obsm['xyz']` — a 90° rotation and a re-centring — which floored `marker_field_r`, `marker_depth_r` and `celltype_localization` while leaving the three graph-internal metrics **bit-identical**, so half a table moved and half did not and it read as a modelling result. (2) `_flanking` took the two nearest sections with **no exclusion**, so under `layout_mode=resample` a LOSO fold's generated section was an **exact copy of the fold itself** (0.0 µm, 100% of cell types) — masked by the first defect until it was fixed. Both fixed with guards and regression tests (`FrameMismatchError`; `exclude_z` now reaches the layout). **Unaffected**: bench3's published `paper_*` numbers, the training losses, and R11's `layout_mode=resample` default. **Re-measured on `deep_starmap`: copying beats the generative path on all five live metrics** (four at 6.0–6.3x the within-arm fold spread), `medcpt` has **no** positive and one established negative (`morans_pearson`, 10.0x the fold spread), and `cross-mix` under `resample` was shown to *be* a copy — it matches a model-free "copy the nearest other section" measurement to 0.001. `zinb-flow` reaches ~35% of copying on `marker_depth_r`. `celltype_localization` is structurally inert for every expression-side gate. The three-seed run was never started, which is fortunate: its target margin has since changed sign and then vanished. Audit: [progress/t09_inference_and_calibration.md](progress/t09_inference_and_calibration.md) |
| T10 | Score v25 on bench3 | `specs/10_TASK_benchmark_and_baselines.md` | `eval/metrics.py`, `eval/baselines.py`, `eval/bench3_driver.py`, `eval/resection.py`, `eval/stats.py`, `eval/ceiling.py`, `eval/experiments.py`, `cli.py` | — | TODO — **scope revised 2026-08-20**: `benchmark-pbya-v3` is the instrument, so T10 no longer builds a benchmark. Two bench3 files are touched, both additively (a new wrapper + one appended `METHODS` entry); `evaluate_paper.py` is never touched and its SHA-256 is asserted before and after every run. Reporting is **two-tier**: Tier 1 is STARmap under `paper_2_4_6`, unmodified (headline + direct SpatialZ comparison); Tier 2 is everything needing a modified design. T10 owns what bench3 lacks: the statistics (Wilcoxon/BH/bootstrap CI/Cliff's delta — bench3 has none), the seed and arm dimensions (separate `BENCH_V3_RESULTS` roots), the boundary holdout for R3, the re-sectioning preprocessor, and one control metric. **Pilot first** (STARmap end to end) before any campaign |

`specs/` defines ten implementation tasks (T01–T10); T00 is this scaffolding pass, listed so the
table covers everything that has been done to the repository.

## Gate status

| Gate | Criterion | Status | Report |
|---|---|---|---|
| GATE 1 (T03) | GRF prior halves median Moran's I error vs i.i.d.; per-gene r > 0.7; `I_gen` monotone in `ell` over the calibration bracket; `I_gen(ell)` unimodal with its maximiser ≥ the fitted `ell` | **PASSED** — error ratio **0.130** (< 0.5), r **0.917** (> 0.7), smallest step over the bracket **+0.028** (> 0), fitted vs best-matching `ell` **8 %** (< 25 %), unimodality violation **0.000** (< 0.0069), maximiser **2.52×** the fitted `ell` (≥ 1×) | `reports/gate1.md` |
| GATE 2 (T04) | **depth-matched** oblique parity ≥ 0.90 (both arms), plus the interior-only check; held-out z ≥ 0.8 × neighbouring z; `w_z = 0` costs R² at fractional depths 0.2/0.8 but not 0.5; attention entropy > 0.5 log K; augmentation complete (G2.1h) and the draw-noise floor measured (G2.1i) | **PASSED** — G2.1a **0.9547** (≥ 0.90), G2.1b edge-excluded **0.9795**, z-interpolation **1.097** (≥ 0.80), `w_z` ablation **+0.030** at 0.2 / **+0.049** at 0.8 vs **+0.003** at 0.5 (< 0.01), entropy **3.422** nats (> 1.733), all four rotation channels wired, draw σ **0.0168**. Criterion amended in `specs/04` after the escalation came back null — see SPEC_QUESTIONS C16 | `reports/gate2.md` |

## Open risks carried forward

Full table and the R1 / R3 / R4 narratives: **[progress/risks.md](progress/risks.md)**.

| # | State |
|---|---|
| R1 | ~~`ell_z` under-determined by a short stack~~ — **implemented at T09**: remedy 2 (match the *observed* between-section correlation) with remedy 3 as the guard (the variogram fit is the bracket's **upper endpoint**). On the fixture the bracket cannot be closed — generated between-section r **0.6734** against an observed **0.9182**, in **0** bisection iterations — so the calibrator returns `target_unreachable` on the config the selector shipped — an artefact, since `cross-mix` makes the objective **exactly constant** (10 dp across a 15× sweep) and the returned 25 µm was a tie-break; the calibrators now refuse that path. **On the live arm** (`correlated` + `zinb-flow`) `ell_xy` **converges** to 86.4 µm (I_gen 0.4051 vs 0.4102, 2 iterations) and is applied; `ell_z` terminates at the bracket's *top* (364.6 µm) still undershooting (0.8706 vs 0.9182), so the data support **ell_z ≥ 364.6 µm** — a *lower* bound, correcting this project's earlier 'upper bound' phrasing — and `apply_lengthscale` drops it. Remedy 3 holds; remedy 2's target may be unreachable **by construction**, since the objective excludes both sections from retrieval while the observed 0.9182 draws on exactly that shared anatomy. **Open on the measurement, closed on every mechanism** |
| R2 | Retrieval attention near-uniform — **closed** at T06 (0.987 → 0.856 × log K) |
| R3 | The stack's ends reconstruct far worse than its interior — **surfaced at T09**: every emitted section carries `uns["boundary"]`, and the uncertainty gate *does* elevate there (latent variance 0.04762 / 0.04207 / 0.04576 at first / middle / last, i.e. **+13.2 %** and **+8.8 %** over the interior — on the `prior_mode="correlated"` arm the sign **inverts**, so the elevation is a property of the shipped `iid` prior, not of the gate). Still **open** as a quality gap; T10 stratifies the headline metrics by it | **Sharpened at T10 from the prior campaign: `section_2` — held out by the paper protocol itself and bracketed by the stack's *first* section — is the worst-scoring held-out section in 37 of 49 (method, metric) tier-1 cells, for every method including the published ones.** So `paper_2_4_6` is not the purely interior test it is usually described as, and tier-1 numbers carry boundary contamination equally for all methods. Consequences: it is a protocol caveat rather than a per-method bias, it is why the estimator is a median (a fixed penalty on 1 of 3 sections launders straight into a mean), and **it gives SpatialZ's `celltype_localization` win two readings** — better layout, or better robustness to one-sided evidence — which C1 separates by reporting the gap per section (`specs/10` §1, §11.1).
| R4 | **Likelihood/fidelity inversion — the objective is minimised by explaining variance with noise rather than with structure.** Named at T06 by merging B10's open half with T06's own (SPEC_QUESTIONS B10: *"the same pathology — a head fitted by its own likelihood improving at that likelihood while the generated section degrades — on two different heads"*). **T10 adds a third instance and a fourth connected result, on real data, and they share one shape**: in each case a likelihood term is reduced by moving explanatory power out of the *structured* component and into the *unstructured* one, with nothing in the objective opposing the trade. **(i) Layout head, T05/B10** — the Poisson NLL fell **three nats below its value at the true intensity** while the correlation with that truth stayed at **0.00**; the fit put mass in thin sheets at the nominal depths and grew spikes against a fixed Monte-Carlo set. **(ii) Expression head, T06** — 1200 → 2400 steps takes reconstruction 1.5901 → **1.5703** nats while gene-gene Frobenius goes 9.000 → **9.049** and Moran's MAE 0.0287 → **0.0339**. **(iii) Emission decomposition, T10 (new, real data)** — the decoder carries only **15.3 %** of its between-cell variance in the structured mean against real tissue's **62.2 %**, while `mu`'s spatial *pattern* is near-perfect (I = 0.861, above the tissue's own latent at 0.745). The amplitude, not the pattern, is missing, and the ZINB NLL at means in the thousands can be reduced by widening dispersion instead of sharpening `mu`. T09's calibrator fixes the *marginal* mean-variance relation (22.0 % → **5.9 %** relative error) and moves retention **14.4 % → 14.6 %** — matching a marginal only reallocates variance inside the same decomposition. **(iv) Gene-gene covariance, T06 (connected)** — model Frobenius **9.316** against an independent-donor baseline's **7.783**: per-gene marginals can be matched by the noise model while the shared latent carries less, which is the same trade in the covariance direction and is why T08's metric-aware terms could not close it. **Open, and now the single largest open question in the project.** Three unexplained results with one candidate cause; the fix is a change to the emission model or its objective, which is a T06 design decision. `reports/pilot.md` §9-§10; `progress/fixture_limitations.md` §1 records why no fixture measurement could have surfaced (iii) |
| R5 | C14 blocks the open-vocabulary claim — **open**, needs one networked run |
| R6 | `L_cross` vacuous in v25 and harmful when trained — **open**, `w_cross` ships at 0 |
| R11 | ~~**The intensity-field layout is worse than copying on real data**~~ — **CLOSED 2026-08-25: `layout_mode` ships as `resample`**, recorded as a negative result in `specs/05` §4a, `specs/10` §4.5b/§6 (A4 restated), `design/v23_design.md` §3.3, and `progress/fixture_limitations.md` §2. `field` and `hybrid` stay selectable and are reported as A4. The defect it localised stays open and is T05's: the intensity integral's scale, unstable 3.7x across refits of one config and link-coupled. History: — measured at T10's pilot on tier-1 STARmap, holding everything else fixed and swapping only `layout_mode`: `celltype_localization` **0.4252** for `field` against **0.7008** for `resample`, where the model-free `flanking_copy` floor is **0.7765**; `cell_count_ratio` 0.83 -> 0.99 in the same swap. The gap to the floor is ~29x the across-seed envelope. It agrees with T09's merged gate, where **`resample` was the rank winner (3.0), `hybrid` followed (4.2), and `field`'s best cell ranked 7.0** — `hybrid` ships only because it won a tie-break whose margin (0.0344 vs a 0.0335 envelope) was **inside the noise** (R10). The fixture signal was underpowered, not wrong, and real data resolves it the same way. **Open**, and owed to T05/T09: `specs/05`'s headline layout claim is in question, and the `hybrid` tie-break is **flagged in `progress/t09_inference_and_calibration.md` as a decision to revisit** — the question is not which of the three ships but whether the gate may be inherited from the fixture at all, given `specs/09` §3 selects per dataset. Does **not** explain the autocorrelation collapse (R12). **2026-08-24 — the sampler is fixed, the decision is not**: `reports/r11_envelope.md`'s defect 3 made the `field` point pattern *biased* as well as starved, so the numbers above were measured on a sampler that drew from `min(lambda, envelope)`. Option D (grid-multinomial) is implemented, validated against a closed-form intensity, and is the default; the rejection sampler is retained and asserted wrong. **A correct sampler removes a bias, it does not supply a better intensity field** — the three modes must be re-measured on tier-1 STARmap before `layout_mode` is decided, and that fit runs on the campaign machine. **2026-08-25 — re-measured, and it did not change the answer**: on the grid sampler `field` **0.6607** and `hybrid` **0.6692** against `resample` **0.7546** and a 0.7765 copy floor, i.e. still 3.5x and 3.2x the envelope below a model-free copy; correcting the bias buys `field` 0.0599 and `hybrid` 0.0158 (inside the envelope), the ordering is unchanged, and the refit reproduces the pilot on the old sampler to within 0.013 on every arm. The **count** got worse by being unmasked — median `cell_count_ratio` 0.895 -> **5.362**, `section_4` 163 cells placed -> 21 993. `reports/r11_starmap_layout_modes.md`, reading in `progress/risks.md`. **The fixture gate re-run on the corrected sampler returns no answer** (`reports/t09_layout_mode_gate_grid.md`, 3 seeds): a different winner for every seed and a pooled 3-way tie, with the between-arm spread smaller than the across-seed spread on 5 of 6 metrics — so the fixture is uninformative on this gate rather than underpowered, and the real-data measurement is what decides. `progress/t05_layout_head.md` |
| R13 | **`cross-mix` is a copy, and reconstruction headroom is dataset-dependent — both measured model-free.** `cross-mix` under the shipped `layout_mode=resample` tracks a **model-free** "copy the section `_resample_layout` picks" measurement of `marker_depth_r`: **−0.0009 / +0.0099** on `deep_starmap`, **−0.0503 / −0.0258** on tier-1 (looser, as its profile is far noisier). An identification, not a margin — tables quoting `cross-mix` as a method quote a copy. ⚠️ **Headroom over the best copy splits the two datasets**: `deep_starmap` **+0.010…+0.064, median 0.5x** the 0.0335 envelope — *saturated*, no method can beat copying measurably, and its 6.0x+ `expr_mode` margins separate the arms on a task where copying is already at **98%** of the achievable ceiling; tier-1 **+0.155…+0.214, median 5.2x** — *real room*, and **`zinb-flow` uses none of it**, sitting 0.096 below copying at 67% of the ceiling against copying's 81%. So the saturation defence is available for `deep_starmap` and **not** for tier-1. The arms are **closest where the task is hardest** — `zinb-flow` reaches **83%** of copying on the sparse noisy panel, **33%** on the dense one — which makes **tier-1 the informative reconstruction benchmark**. The generative path's remaining case rests on what copying cannot do: **unmeasured genes** — note every `text_emb_mode` measurement is on **fitted** genes, where `lookup` memorises and `medcpt` is strictly more constrained, so *the gate as run cannot favour `medcpt` even in principle*; `forward_zero_shot` and `gene_pool` exist and have never been run — and **arbitrary planes**, where the comparison is categorical because copying has no output. `reports/t09_depth_ceiling_{starmap,deep}.md` |
| R12 | **Spatial autocorrelation collapses in the expression path on real data** — the generated field carries **15 %** of STARmap's per-gene Moran's I (median 0.0703 against 0.4635), and **22 %** even when placed at real positions. Not an inversion: the predicted values are squashed into a narrow band, so `paper_morans_pearson` is noise about zero and its negative sign is meaningless — the interpretable quantity is `morans_median_pred` vs `morans_median_gt`. **Length-scale calibration and the layout head are both ruled out by direct experiment** (T10 pilot §6.2, §6.3). Remaining candidates: the step budget, and `text_emb_mode="medcpt"` which is testable only on the campaign machine. **The campaign is on hold until this is attributed** — a 200+ hour run against a 15-22 % field would measure the defect at scale. **LOCALISED at the pilot**: the chain measures median Moran's I of **0.9714** at the GRF prior, **0.9015** after the flow and **0.8607** at the decoded `mu` — then **0.1297** at the sampled counts. Stages 1-3 lose 0.11 in total; the count draw loses **0.73 in one operation**. Real tissue retains **62 %** across the same latent->counts step (0.7449 -> 0.4635); the model retains **14 %**. The output is not globally over-dispersed (variance 0.57x real, zero fraction 0.0075 vs 0.0068) — the mean-variance **slope** is wrong, 2.120 against 1.738, a 22 % error against T06's < 0.15 criterion, i.e. `mu` is spatially smooth but too flat in amplitude for its structure to survive sampling. **The budget is also ruled out** (14.1 % -> 18.4 % at 2x steps). **Calibration measured and insufficient — this is a DESIGN finding, not a tuning one.** T09 §2's `calibrate_detection` fits in 7.4 s and does exactly its job: the mean-variance slope goes 2.121 -> **1.840** against the tissue's 1.738, a relative error of 22.0 % -> **5.9 %**, passing T06's < 15 % criterion. **Retention does not move: 14.4 % -> 14.6 %** against the tissue's 62.2 %. Since the draw is spatially uncorrelated by construction, `I(counts)/I(mu)` is the share of between-cell variance carried by the structured mean: **15.3 % for the model against 62.2 % for real tissue.** The decoder reproduces the *pattern* of between-cell variation almost perfectly (`mu` at 0.861, above the tissue's own latent at 0.745) at a fraction of its *amplitude*, and fills the gap with dispersion — and calibrating `theta` matches a marginal, which only reallocates variance inside the same decomposition. Not the prior, the flow, the budget, the layout, calibration, or `text_emb_mode`. **Owed to T06**: why does `mu` carry only 15 % of between-cell variance? Candidates — the size-factor path, `decoder_mu_link` (softplus compresses dynamic range at means in the thousands, a regime the fixture never tested), and a ZINB NLL that can be reduced by widening dispersion rather than sharpening `mu` (connects to R4 and T06's covariance loss). **Candidate 1 recovered it**: `Config.decoder_mu_link="exp"` takes the structured share **15.1 % -> 61.4 % at GT-matched density**, with counts Moran's I **+0.1297 -> +0.4782** against real tissue's **+0.4635**. Density-controlled on the saved model (11.5x -> 1.0x subsamples; the share is flat at 58-61 %, so the gain is not a density artifact). Candidate 2 is **ruled out** — the size factor supplies **0.2 %** of `Var(log mu)`, the latent 100 %. ⚠️ `ZINBDecoder`'s docstring says `exp` is the default while `Config` ships `softplus`, so this may be a **defaulting error** rather than a design choice; **owed to T06**. Caveats: the `exp` arm emits 48 343 cells against a GT of 4 187 (R11, independent), `sd(log mu)` 0.777 is still below the tissue's 1.213, T09's calibrator now *overshoots* the slope and should be re-evaluated rather than inherited, and retention is **not comparable across arms** because its denominator is that arm's own encoder. **Campaign remains on hold** pending the T06 decision and R11 |
| R7 | SEFL's net contribution unverified — **open**, decided by T10's A7 |
| R8 | ~~Reduced-epoch selection reverses gates with a training-free option~~ — **RESOLVED at T09**: `specs/09` §3 now carries the **training-free-option rule**, enforced by `TRAINING_FREE_OPTIONS` + an import-time check, merging `layout_mode` x `prior_mode` x `expr_mode` into one 18-cell full-budget gate. All six `cross-mix` cells rank bottom; **all three gates changed answer**, `layout_mode` included. New config `00ef4a19a2f576b8`; the donor-bar score goes **2/6 → 4/6** |
| R9 | ~~A gate decided on an unconverged incumbent~~ — **RESOLVED at T09**: `specs/09` §3's rule gained condition (2), *measured* per run — escalate every remaining gate when the incumbent falls by more than `selection_convergence_tol` on ≥2 metrics at the reduced budget. It fires here (shortfalls 0.36 / 0.43 / 0.81). Re-scored at 2400, `lookup` **still wins** (1.2 vs 1.8) — the winner does not flip back |
| R10 | ~~Scores reproducible only to ~0.012~~ — **RESOLVED at T09 as a rule; envelope now measured**: 3 cells x 3 seeds give **0.0335**, ~3x the n=2 figure, not score-dependent but 5x metric-dependent. Ships as two rules — repeated seeds (`claim_min_seeds` = 3, scoped in `specs/10` §3, ~2.4x campaign bill) and the capability tie-break (`claim_tie_break_envelope` = 0.04). Re-check found **`layout_mode` decided inside the noise** (0.0344 vs 0.0335) → `hybrid`, and `text_emb_mode` → `medcpt`. Shipped `00ef4a19a2f576b8` |

## Numbers the paper needs

**[progress/numbers.md](progress/numbers.md)** — the per-task table, filled in as tasks land.

## Log

One file per task. Each records what was built, the measured numbers, and every
deviation from the spec with its reason.

| Task | Entry |
|---|---|
| T00 | [progress/t00_scaffolding.md](progress/t00_scaffolding.md) |
| T01 | [progress/t01_config_and_data.md](progress/t01_config_and_data.md) |
| T02 | [progress/t02_text_embeddings.md](progress/t02_text_embeddings.md) — includes the two `build_gene_meta` repairs and the B20 orthologue fallback |
| T03 | [progress/t03_noise_field.md](progress/t03_noise_field.md) — GATE 1, failed then re-run and passed |
| T04 | [progress/t04_field_and_retrieval.md](progress/t04_field_and_retrieval.md) — GATE 2, the escalation, and the per-query z window |
| T05 | [progress/t05_layout_head.md](progress/t05_layout_head.md) |
| T06 | [progress/t06_expression_head.md](progress/t06_expression_head.md) — includes the covariance demotion and C14's escalation |
| T07 | [progress/t07_sefl_losses.md](progress/t07_sefl_losses.md) |
| T08 | [progress/t08_metric_aware.md](progress/t08_metric_aware.md) |
| R8 | [reports/r8_budget_grid.md](reports/r8_budget_grid.md) — the 2 x 3 x 2 budget grid, the rank reversals, and the gap on record |
| R10 | [reports/envelope_synthetic.md](reports/envelope_synthetic.md) — the measured envelope and which gate choices fell inside it |
| T09 | [progress/t09_inference_and_calibration.md](progress/t09_inference_and_calibration.md) — the joint gate's four cells, the calibration statuses, R1/R3, and the two negative results |
| R11 | [progress/t05_layout_head.md](progress/t05_layout_head.md#t05-addendum--the-grid-multinomial-position-sampler-2026-08-24) — option D, the closed-form validation, and T05's re-measured numbers |
| — | [progress/durability_checkpointing.md](progress/durability_checkpointing.md) — fit checkpointing, and the bitwise-identity acceptance test that keeps Convention 3 true |
| — | [progress/decisions.md](progress/decisions.md) — spec decisions settled with no code change |

**Appending to this log:** add the entry to the task's own file under `progress/`,
update the row in the status table above, and leave this index short. The file was
split at T08 because a 2131-line PROGRESS.md could no longer be rewritten in one pass,
which is how a task's findings came to be missing from it.
