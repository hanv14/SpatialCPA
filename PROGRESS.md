# PROGRESS — SpatialCPA-v25-Gen

Status of every task in `specs/`. Update the row when a task lands, then append an entry under
"Log" with what was built, the test numbers, and any deviation from the spec and why.

Status values: `TODO` | `IN PROGRESS` | `BLOCKED` | `DONE`.

| # | Task | Spec | Key deliverables | Gate | Status |
|---|---|---|---|---|---|
| T00 | Project scaffolding | — | `CLAUDE.md`, `PROGRESS.md`, `pyproject.toml`, `Makefile`, ruff/mypy/pytest config, `SPEC_QUESTIONS.md` | — | DONE |
| T01 | Config and data contracts | `specs/01_TASK_config_and_data.md` | `config.py`, `data/schema.py`, `data/loaders.py`, synthetic fixture | — | DONE |
| T02 | Text-grounded embeddings | `specs/02_TASK_text_embeddings.md` | `data/text.py`, `model/embeddings.py`, MedCPT cache, distillation head | — | DONE |
| T03 | 3D GRF noise field | `specs/03_TASK_noise_field_GATE1.md` | `model/noise.py`, `scripts/gate1_report.py`, `reports/gate1.md` | **GATE 1** | DONE — **GATE 1 passes** on the 3000 µm gate fixture |
| T04 | Anatomical field + retrieval | `specs/04_TASK_field_and_retrieval_GATE2.md` | `model/field.py`, `model/retrieval.py`, `scripts/gate2_report.py`, `reports/gate2.md` | **GATE 2** | DONE — **GATE 2 passes**, depth-matched oblique parity **0.955** (edge-excluded check **0.979**); amended 2026-08-16 with the per-query z window (SPEC_QUESTIONS C1c), **no gate criterion changed verdict** — restated 2026-08-25 from the wider "gate numbers unchanged", which was measured on the *evaluation* path (`test_gap_relative_window_is_identity_on_a_regular_stack`, bitwise) and does not cover `G2.1h-c`'s off-stack mutation arm, where the amendment moved the reported value 40.28 -> 63.97 (C32, reproduced exactly). Audit: [progress/t04_field_and_retrieval.md](progress/t04_field_and_retrieval.md) |
| T05 | Layout head | `specs/05_TASK_layout_head.md` | `model/layout.py`, `losses/reconstruction.py` (layout NLL), `infer/planes.py` (minimal `Plane`), intensity + Strauss sampler + Potts marks | — | DONE — all eight acceptance tests pass, both negative controls fail as they must. **Amended 2026-08-24**: the position sampler for `field` / `hybrid` was measurably biased (`reports/r11_envelope.md` defect 3 — a sampled-maximum envelope with an unclamped acceptance ratio, so the draw came from `min(lambda, envelope)`). `reports/r11_fix_options.md`'s **option D** — a grid-multinomial sampler with no envelope, no acceptance ratio and a closed-form expected count per cell — is now the default (`Config.layout_sampler="grid"`); `"rejection"` stays selectable and is asserted *wrong* as the negative control. Validated against a closed-form intensity at max/mean **235** with **no fit and no data** (16 tests). T05's own numbers re-measured: generated/ideal **97.8 %** (was 99.4 % on the biased sampler), generated/flanking 1.491 / 1.514 / 1.059 |
| T06 | Expression head + ZINB decoder | `specs/06_TASK_expression_head.md` | `model/expression.py`, `model/spatialcpav25_gen.py` (`CTFFlow` + trainer), `losses/reconstruction.py`, `eval/baselines.py` | — | DONE — with three recorded failures: the covariance criterion is **unsatisfiable as stated** (below the ceiling, B16) and the model half of the amendment does **not** hold out of sample; zero-shot decoding is **r = −0.368** (B18); T05's intensity overfit is answered at trainer level but not abolished (R4) |
| T07 | SEFL consistency losses | `specs/07_TASK_sefl_losses.md` | `losses/sefl.py`, `infer/planes.py`, EMA teacher, collapse alarm | — | DONE — **SEFL ships opt-in (all three weights 0)**, with one result that reframes the loss it belongs to: intersection consistency is **exact by construction** in v25 (bitwise, untrained), so `L_cross` has only the augmentation pose left to constrain and minimising that flattens the field (generated per-gene variance **0.065** against **0.711** with SEFL off). `w_cross` ships at **0**, the failure is a strict xfail, and the decision is R6 / SPEC_QUESTIONS C19. `L_thick` and `L_prog` land as specified but are **also off**, because at their spec weights they break three of T06's acceptance criteria (R7); A7 at T10 decides whether SEFL is used at all |
| T08 | Metric-aware LOSO losses | `specs/08_TASK_metric_aware_losses.md` | `losses/metric_aware.py`, `train/loso.py`, `scripts/t08_metric_report.py` | — | DONE — with **both** headline criteria red and the decisions they force taken: the terms **ship at 0** (opt-in, A2 becomes an addition experiment) because at T06's 1200-step budget they cost on every metric they are made of, and open risk **R4's covariance criterion is not met on either holdout** (11.02 vs a 7.73 baseline at `alternating`, 13.39 vs 11.38 at `consecutive-3`), so the paper's covariance claim is **downgraded to a mechanism claim** in `specs/10` §2. The finding that qualifies both: at **2400** steps the ordering reverses on four of six statistics — the terms are slower, not worse, and 1200 steps is R4's own symptom |
| T09 | Inference + calibration | `specs/09_TASK_inference_and_calibration.md` | `infer/generate.py`, `infer/calibrate.py`, `train/select.py`, `scripts/t09_report.py`, `reports/config_selection_synthetic.md` | — | DONE — **the joint gate decided what T08 deferred: `train_steps = 2400` with all three metric-aware weights at 0.5** (the weights lose at 1x, rank 3.5 vs 3.0, and win at 2x, 1.0 vs 2.0, on four of six metrics — the cell a one-gate-at-a-time selector cannot reach). Four results the run forced: the reduced-budget scoring first switched `prior_mode` to **iid** and `expr_mode` to **cross-mix**, which R8 then showed to be an artefact of the 25 % budget — after the **training-free-option rule** merged `layout_mode` x `prior_mode` x `expr_mode` into one 18-cell full-budget gate, the selection is **`resample` + `correlated` + `zinb-flow` + `lookup`** (`00ef4a19a2f576b8`) and the definition of done against the independent-donor bar goes **2 of 6 → 4 of 6**; the calibration on the *selected* config was an **artefact of a flat objective** — `cross-mix` never evaluates the flow, so `ell` cannot move a single emitted count — and the calibrators now refuse that path as well as `iid`, while `apply_lengthscale` (C30) writes a **converged** axis through to the prior and drops the rest; on the live arm `ell_xy` converges to **86.4 µm** and the anisotropy is bounded at **ell_z/ell_xy ≥ 4.2**; E5's expression criterion is **above its achievable ceiling** (0.724 measured against a ceiling of 0.726 and a spec threshold of 0.85 — strict xfail); and the definition of done, **on the reselected config**, is met on 4 of 6 against both `resample` and the independent-donor baseline (0.9517 / 0.9142 / 0.9730 on the three distribution-level metrics), with one tie and one loss each. **2026-08-26 — the machinery for the per-dataset run on tier-1 STARmap is built and the campaign is not yet run.** The finding that made it necessary: **`text_emb_mode="medcpt"` had never been exercised on real data and could not have been** — all five embedding builders passed either zeros or `gene_descriptor(symbol, None)`, so the encoder saw the bare string `"Slc17a7."` while `resources/gene_meta.parquet` carried a full name and an NCBI summary for **28/28** of the panel; every STARmap number to date is ablation A3's `lookup` arm under another name. `model.build_entity_embeddings` is now the single builder and both arms of the gate get the same vectors. Four defects that would each have stopped or spoiled the run are fixed: selection through the bench3 wrapper **raised at its first line** (no embeddings factory, so no per-dataset selection had ever run on any dataset); `expr_pca_dim = 32` exceeded the 28-gene panel (**the owed fix, now `clamp_config_to_volume`** — the rule gives 28, where the pilot's 16 was a stand-in); `fit_repulsion` was paid for under `layout_mode=resample`, which never reads it and where it *raises*; and one under-trained candidate aborted the whole search, where `assert_detection_rate`'s band against STARmap's 0.9999 detection makes that the expected case — such a candidate now ranks last, loudly, and a gate that fails entirely is refused. Two mechanisms `specs/09` §3 describes and nothing implemented are now wired: the **capability tie-break** (applied by hand to a printed table at §13, so a persisted `selected.yaml` would have carried the rank winner) and **pinned gates** with a required reason. **2026-08-26 — both real-data audits are in, and both are negative.** On tier-1 STARmap `lookup` beats `medcpt` by **1.3x** the 0.0335 envelope on Moran's and Geary's (the first real measurement of that gate at all), and `cross-mix` beats `zinb-flow` by **4.6–5.3x** on Moran's, Geary's and `umap_mixing` while tying inside the envelope on all three arrangement metrics — i.e. **copying wins exactly the three metrics T06/T09 predicted a copy would win** (real donor counts maximise per-gene autocorrelation and shared-embedding mixing) and buys nothing where a generative advantage would show. One seed, **2 folds**, T08 kernels on internal LOSO — not `bench3.evaluate_paper`. **2026-08-27 — two defects invalidated the arrangement half of every internal-LOSO number, and fixing them inverted the reading.** (1) `section_scores` was handed the *plane-local* `(u,v)` coordinates a generated `AnnData` carries at `obsm[coord_key]` instead of the physical `obsm['xyz']` — a 90° rotation and a re-centring — which floored `marker_field_r`, `marker_depth_r` and `celltype_localization` while leaving the three graph-internal metrics **bit-identical**, so half a table moved and half did not and it read as a modelling result. (2) `_flanking` took the two nearest sections with **no exclusion**, so under `layout_mode=resample` a LOSO fold's generated section was an **exact copy of the fold itself** (0.0 µm, 100% of cell types) — masked by the first defect until it was fixed. Both fixed with guards and regression tests (`FrameMismatchError`; `exclude_z` now reaches the layout). **Unaffected**: bench3's published `paper_*` numbers, the training losses, and R11's `layout_mode=resample` default. **Re-measured on `deep_starmap`: copying beats the generative path on all five live metrics** (four at 6.0–6.3x the within-arm fold spread), `medcpt` has **no** positive and one established negative (`morans_pearson`, 10.0x the fold spread), and `cross-mix` under `resample` was shown to *be* a copy — it matches a model-free "copy the nearest other section" measurement to 0.001. `zinb-flow` reaches ~35% of copying on `marker_depth_r`. `celltype_localization` is structurally inert for every expression-side gate. The three-seed run was never started, which is fortunate: its target margin has since changed sign and then vanished. Audit: [progress/t09_inference_and_calibration.md](progress/t09_inference_and_calibration.md) |
| T10 | Score v25 on bench3 | `specs/10_TASK_benchmark_and_baselines.md` | `eval/metrics.py`, `eval/baselines.py`, `eval/bench3_driver.py`, `eval/resection.py`, `eval/stats.py`, `eval/ceiling.py`, `eval/experiments.py`, `cli.py` | — | TODO — **scope revised 2026-08-20**: `benchmark-pbya-v3` is the instrument, so T10 no longer builds a benchmark. Two bench3 files are touched, both additively (a new wrapper + one appended `METHODS` entry); `evaluate_paper.py` is never touched and its SHA-256 is asserted before and after every run. Reporting is **two-tier**: Tier 1 is STARmap under `paper_2_4_6`, unmodified (headline + direct SpatialZ comparison); Tier 2 is everything needing a modified design. T10 owns what bench3 lacks: the statistics (Wilcoxon/BH/bootstrap CI/Cliff's delta — bench3 has none), the seed and arm dimensions (separate `BENCH_V3_RESULTS` roots), the boundary holdout for R3, the re-sectioning preprocessor, and one control metric. **Pilot first** (STARmap end to end) before any campaign |

`specs/` defines ten implementation tasks (T01–T10); T00 is this scaffolding pass, listed so the
table covers everything that has been done to the repository.

## Gate status

| Gate | Criterion | Status | Report |
|---|---|---|---|
| GATE 1 (T03) | GRF prior halves median Moran's I error vs i.i.d.; per-gene r > 0.7; `I_gen` monotone in `ell` over the calibration bracket; `I_gen(ell)` unimodal with its maximiser ≥ the fitted `ell` | **PASSED** — error ratio **0.130** (< 0.5), r **0.917** (> 0.7), smallest step over the bracket **+0.028** (> 0), fitted vs best-matching `ell` **8 %** (< 25 %), unimodality violation **0.000** (< 0.0069), maximiser **2.52×** the fitted `ell` (≥ 1×) | `reports/gate1.md` |
| GATE 2 (T04) | **depth-matched** oblique parity ≥ 0.90 (both arms), plus the interior-only check; held-out z ≥ 0.8 × neighbouring z; `w_z = 0` costs R² at fractional depths 0.2/0.8 but not 0.5; attention entropy > 0.5 log K; augmentation complete (G2.1h) and the draw-noise floor measured (G2.1i) | **PASSED** — G2.1a **0.9547** (≥ 0.90), G2.1b edge-excluded **0.9795**, z-interpolation **1.097** (≥ 0.80), `w_z` ablation **+0.030** at 0.2 / **+0.049** at 0.8 vs **+0.003** at 0.5 (< 0.01), entropy **3.422** nats (> 1.733), all four rotation channels wired, draw σ **0.0168**. Criterion amended in `specs/04` after the escalation came back null — see SPEC_QUESTIONS C16 | `reports/gate2.md` |

## Open risks carried forward

Full table and the R1 / R3 / R4 narratives: **[progress/risks.md](progress/risks.md)**.

| # | State |
|---|---|
| R1 | ~~`ell_z` under-determined by a short stack~~ — **implemented at T09**: remedy 2 (match the *observed* between-section correlation) with remedy 3 as the guard (the variogram fit is the bracket's **upper endpoint**). On the fixture the bracket cannot be closed — generated between-section r **0.6734** against an observed **0.9182**, in **0** bisection iterations — so the calibrator returns `target_unreachable` on the config the selector shipped — an artefact, since `cross-mix` makes the objective **exactly constant** (10 dp across a 15× sweep) and the returned 25 µm was a tie-break; the calibrators now refuse that path. **On the live arm** (`correlated` + `zinb-flow`) `ell_xy` **converges** to 86.4 µm (I_gen 0.4051 vs 0.4102, 2 iterations) and is applied; `ell_z` terminates at the bracket's *top* (364.6 µm) still undershooting (0.8706 vs 0.9182), so the data support **ell_z ≥ 364.6 µm** — a *lower* bound, correcting this project's earlier 'upper bound' phrasing — and `apply_lengthscale` drops it. Remedy 3 holds; remedy 2's target may be unreachable **by construction**, since the objective excludes both sections from retrieval while the observed 0.9182 draws on exactly that shared anatomy. **Open on the measurement, closed on every mechanism** |
| R2 | Retrieval attention near-uniform — **closed** at T06 (0.987 → 0.856 × log K) |
| R3 | The stack's ends reconstruct far worse than its interior — **surfaced at T09**: every emitted section carries `uns["boundary"]`, and the uncertainty gate *does* elevate there (latent variance 0.04762 / 0.04207 / 0.04576 at first / middle / last, i.e. **+13.2 %** and **+8.8 %** over the interior — on the `prior_mode="correlated"` arm the sign **inverts**, so the elevation is a property of the shipped `iid` prior, not of the gate). Still **open** as a quality gap; T10 stratifies the headline metrics by it | **Sharpened at T10 from the prior campaign: `section_2` — held out by the paper protocol itself and bracketed by the stack's *first* section — is the worst-scoring held-out section in 37 of 49 (method, metric) tier-1 cells, for every method including the published ones.** So `paper_2_4_6` is not the purely interior test it is usually described as, and tier-1 numbers carry boundary contamination equally for all methods. Consequences: it is a protocol caveat rather than a per-method bias, it is why the estimator is a median (a fixed penalty on 1 of 3 sections launders straight into a mean), and **it gives SpatialZ's `celltype_localization` win two readings** — better layout, or better robustness to one-sided evidence — which C1 separates by reporting the gap per section (`specs/10` §1, §11.1).
| R4 | **Likelihood/fidelity inversion — the objective is minimised by explaining variance with noise rather than with structure.** Named at T06 by merging B10's open half with T06's own (SPEC_QUESTIONS B10: *"the same pathology — a head fitted by its own likelihood improving at that likelihood while the generated section degrades — on two different heads"*). **T10 adds a third instance and a fourth connected result, on real data, and they share one shape**: in each case a likelihood term is reduced by moving explanatory power out of the *structured* component and into the *unstructured* one, with nothing in the objective opposing the trade. **(i) Layout head, T05/B10** — the Poisson NLL fell **three nats below its value at the true intensity** while the correlation with that truth stayed at **0.00**; the fit put mass in thin sheets at the nominal depths and grew spikes against a fixed Monte-Carlo set. **(ii) Expression head, T06** — 1200 → 2400 steps takes reconstruction 1.5901 → **1.5703** nats while gene-gene Frobenius goes 9.000 → **9.049** and Moran's MAE 0.0287 → **0.0339**. **(iii) Emission decomposition, T10 (new, real data)** — the decoder carries only **15.3 %** of its between-cell variance in the structured mean against real tissue's **62.2 %**, while `mu`'s spatial *pattern* is near-perfect (I = 0.861, above the tissue's own latent at 0.7449 — ⚠️ a comparison between two latents, measured on the encoder's latent for a real section, not on the flow's latent at generated positions). The amplitude, not the pattern, is missing, and the ZINB NLL at means in the thousands can be reduced by widening dispersion instead of sharpening `mu`. T09's calibrator fixes the *marginal* mean-variance relation (22.0 % → **5.9 %** relative error) and moves retention **14.4 % → 14.6 %** — matching a marginal only reallocates variance inside the same decomposition. **(iv) Gene-gene covariance, T06 (connected)** — model Frobenius **9.316** against an independent-donor baseline's **7.783**: per-gene marginals can be matched by the noise model while the shared latent carries less, which is the same trade in the covariance direction and is why T08's metric-aware terms could not close it. **Open, and now the single largest open question in the project.** Three unexplained results with one candidate cause; the fix is a change to the emission model or its objective, which is a T06 design decision. `reports/pilot.md` §9-§10; `progress/fixture_limitations.md` §1 records why no fixture measurement could have surfaced (iii) |
| R5 | C14 blocks the open-vocabulary claim — **open**, needs one networked run |
| R6 | `L_cross` vacuous in v25 and harmful when trained — **open**, `w_cross` ships at 0 |
| R11 | ~~**The intensity-field layout is worse than copying on real data**~~ — **CLOSED 2026-08-25: `layout_mode` ships as `resample`**, recorded as a negative result in `specs/05` §4a, `specs/10` §4.5b/§6 (A4 restated), `design/v23_design.md` §3.3, and `progress/fixture_limitations.md` §2. `field` and `hybrid` stay selectable and are reported as A4. The defect it localised stays open and is T05's: the intensity integral's scale, unstable 3.7x across refits of one config and link-coupled. History: — measured at T10's pilot on tier-1 STARmap, holding everything else fixed and swapping only `layout_mode`: `celltype_localization` **0.4252** for `field` against **0.7008** for `resample`, where the model-free `flanking_copy` floor is **0.7765**; `cell_count_ratio` 0.83 -> 0.99 in the same swap. The gap to the floor is ~29x the across-seed envelope. It agrees with T09's merged gate, where **`resample` was the rank winner (3.0), `hybrid` followed (4.2), and `field`'s best cell ranked 7.0** — `hybrid` ships only because it won a tie-break whose margin (0.0344 vs a 0.0335 envelope) was **inside the noise** (R10). The fixture signal was underpowered, not wrong, and real data resolves it the same way. **Open**, and owed to T05/T09: `specs/05`'s headline layout claim is in question, and the `hybrid` tie-break is **flagged in `progress/t09_inference_and_calibration.md` as a decision to revisit** — the question is not which of the three ships but whether the gate may be inherited from the fixture at all, given `specs/09` §3 selects per dataset. Does **not** explain the autocorrelation collapse (R12). **2026-08-24 — the sampler is fixed, the decision is not**: `reports/r11_envelope.md`'s defect 3 made the `field` point pattern *biased* as well as starved, so the numbers above were measured on a sampler that drew from `min(lambda, envelope)`. Option D (grid-multinomial) is implemented, validated against a closed-form intensity, and is the default; the rejection sampler is retained and asserted wrong. **A correct sampler removes a bias, it does not supply a better intensity field** — the three modes must be re-measured on tier-1 STARmap before `layout_mode` is decided, and that fit runs on the campaign machine. **2026-08-25 — re-measured, and it did not change the answer**: on the grid sampler `field` **0.6607** and `hybrid` **0.6692** against `resample` **0.7546** and a 0.7765 copy floor, i.e. still 3.5x and 3.2x the envelope below a model-free copy; correcting the bias buys `field` 0.0599 and `hybrid` 0.0158 (inside the envelope), the ordering is unchanged, and the refit reproduces the pilot on the old sampler to within 0.013 on every arm. The **count** got worse by being unmasked — median `cell_count_ratio` 0.895 -> **5.362**, `section_4` 163 cells placed -> 21 993. `reports/r11_starmap_layout_modes.md`, reading in `progress/risks.md`. **The fixture gate re-run on the corrected sampler returns no answer** (`reports/t09_layout_mode_gate_grid.md`, 3 seeds): a different winner for every seed and a pooled 3-way tie, with the between-arm spread smaller than the across-seed spread on 5 of 6 metrics — so the fixture is uninformative on this gate rather than underpowered, and the real-data measurement is what decides. `progress/t05_layout_head.md` |
| R14 | **`_resample_layout` picks its donor by `|dz|` alone, and on `deep_starmap` that is the wrong donor by 0.116.** `layout_mode=resample` is the **shipped** default and reuses the nearest flanking section's coordinates and cell types; `nearest = min(sections, key=(abs(dz), section_id))`. On `deep_starmap` `section_5` that rule takes **`section_7` at 40.6 µm (`marker_depth_r` 0.8578)** over **`section_3` at 42.0 µm (0.9739)** — it pays **0.116 of score for 1.4 µm of proximity**, 3.5x the 0.0335 envelope. Measured model-free (`reports/t09_depth_ceiling_deep.md`), so it is a property of the rule, not of any fit. **Consequence**: the headroom over the copy the system *actually performs* is **+0.0855 [+0.0489, +0.1351] = 2.6x the envelope** on `deep_starmap`, against **+0.0160 [+0.0104, +0.0243] = 0.5x** over the best available copy — i.e. `deep_starmap` is saturated against an *oracle* copier and **not** against ours. This is a defect in the shipped configuration with a quantified cost, not a baselines footnote: every 'copying wins' number is measured against a copier leaving 2.6x the envelope on the table, and a reader may fairly discount it as beating a weak baseline. **The better rule**: choose the donor by **profile correlation** to the target (or by niche similarity — mean retrieval-feature distance), not by `|dz|`. Both are computable from the training volume at generation time and neither needs a fit; the ceiling instrument already measures the first for every donor pair. ⚠️ **Not changed now, deliberately** — altering `_resample_layout` would invalidate every number stabilised by the coordinate-frame and layout-leak fixes, including the tier-1 and `deep_starmap` audits and both ceiling runs. Fix it *after* the envelope re-measurement, and re-run the audits against it. `reports/t09_depth_ceiling_deep.md`, `specs/10` §0a |
| R13 | **`cross-mix` is a copy, and reconstruction headroom is dataset-dependent — both measured model-free.** `cross-mix` under the shipped `layout_mode=resample` tracks a **model-free** "copy the section `_resample_layout` picks" measurement of `marker_depth_r`: **−0.0009 / +0.0099** on `deep_starmap`, **−0.0503 / −0.0258** on tier-1 (looser, as its profile is far noisier). An identification, not a margin — tables quoting `cross-mix` as a method quote a copy. ⚠️ **Headroom over the best copy splits the two datasets, and a 400-replicate bootstrap over cell subsamples and marker-gene resamples confirms it**: `deep_starmap` **+0.0160 [+0.0104, +0.0243] = 0.5x** the 0.0335 envelope — *saturated against an oracle copier* (**not** against ours: see **R14**, +0.0855 = 2.6x), its 6.0x+ `expr_mode` margins separating the arms on a task where copying already reaches **98%** of the achievable ceiling; tier-1 **+0.1551 [+0.1075, +0.2186] = 4.6x** — *real room*, and **`zinb-flow` uses none of it**, sitting 0.096 below copying at 67% of the ceiling against copying's 81%. So the saturation defence is available for `deep_starmap` and **not** for tier-1. The arms are **closest where the task is hardest** — `zinb-flow` reaches **83%** of copying on the sparse noisy panel, **33%** on the dense one — which makes **tier-1 the informative reconstruction benchmark**. The generative path's remaining case rests on what copying cannot do: **unmeasured genes** — note every `text_emb_mode` measurement is on **fitted** genes, where `lookup` memorises and `medcpt` is strictly more constrained, so *the gate as run cannot favour `medcpt` even in principle*; `forward_zero_shot` and `gene_pool` exist and have never been run — and **arbitrary planes**, where the comparison is categorical because copying has no output. **Intervals disjoint, P(deep > tier-1) = 0.000, difference −0.1389 [−0.2017, −0.0884]** — the reversal is established, and is now `specs/10` §0a. `reports/t09_depth_ceiling_{starmap,deep}.md`, `reports/t09_ceiling_bootstrap_{starmap,deep}.md` |
| R12 | **Spatial autocorrelation collapses in the expression path on real data** — the generated field carries **15 %** of STARmap's per-gene Moran's I (median 0.0703 against 0.4635), and **22 %** even when placed at real positions. Not an inversion: the predicted values are squashed into a narrow band, so `paper_morans_pearson` is noise about zero and its negative sign is meaningless — the interpretable quantity is `morans_median_pred` vs `morans_median_gt`. **Length-scale calibration and the layout head are both ruled out by direct experiment** (T10 pilot §6.2, §6.3). Remaining candidates: the step budget, and `text_emb_mode="medcpt"` which is testable only on the campaign machine. **The campaign is on hold until this is attributed** — a 200+ hour run against a 15-22 % field would measure the defect at scale. **LOCALISED at the pilot**: the chain measures median Moran's I of **0.9714** at the GRF prior, **0.9015** after the flow and **0.8607** at the decoded `mu` — then **0.1297** at the sampled counts. Stages 1-3 lose 0.11 in total; the count draw loses **0.73 in one operation**. Real tissue retains **62 %** across the same latent->counts step (0.7449 -> 0.4635); the model retains **14 %**. The output is not globally over-dispersed (variance 0.57x real, zero fraction 0.0075 vs 0.0068) — the mean-variance **slope** is wrong, 2.120 against 1.738, a 22 % error against T06's < 0.15 criterion, i.e. `mu` is spatially smooth but too flat in amplitude for its structure to survive sampling. **The budget is also ruled out** (14.1 % -> 18.4 % at 2x steps). **Calibration measured and insufficient — this is a DESIGN finding, not a tuning one.** T09 §2's `calibrate_detection` fits in 7.4 s and does exactly its job: the mean-variance slope goes 2.121 -> **1.840** against the tissue's 1.738, a relative error of 22.0 % -> **5.9 %**, passing T06's < 15 % criterion. **Retention does not move: 14.4 % -> 14.6 %** against the tissue's 62.2 %. Since the draw is spatially uncorrelated by construction, `I(counts)/I(mu)` is the share of between-cell variance carried by the structured mean: **15.3 % for the model against 62.2 % for real tissue.** The decoder reproduces the *pattern* of between-cell variation almost perfectly (`mu` at 0.861, above the tissue's own latent at 0.7449 — ⚠️ a comparison between two latents, measured on the encoder's latent for a real section, not on the flow's latent at generated positions) at a fraction of its *amplitude*, and fills the gap with dispersion — and calibrating `theta` matches a marginal, which only reallocates variance inside the same decomposition. Not the prior, the flow, the budget, the layout, calibration, or `text_emb_mode`. **Owed to T06**: why does `mu` carry only 15 % of between-cell variance? Candidates — the size-factor path, `decoder_mu_link` (softplus compresses dynamic range at means in the thousands, a regime the fixture never tested), and a ZINB NLL that can be reduced by widening dispersion rather than sharpening `mu` (connects to R4 and T06's covariance loss). **Candidate 1 recovered it**: `Config.decoder_mu_link="exp"` takes the structured share **15.1 % -> 61.4 % at GT-matched density**, with counts Moran's I **+0.1297 -> +0.4782** against real tissue's **+0.4635**. Density-controlled on the saved model (11.5x -> 1.0x subsamples; the share is flat at 58-61 %, so the gain is not a density artifact). Candidate 2 is **ruled out** — the size factor supplies **0.2 %** of `Var(log mu)`, the latent 100 %. ⚠️ `ZINBDecoder`'s docstring says `exp` is the default while `Config` ships `softplus`, so this may be a **defaulting error** rather than a design choice; **owed to T06**. Caveats: the `exp` arm emits 48 343 cells against a GT of 4 187 (R11, independent), `sd(log mu)` 0.777 is still below the tissue's 1.213, T09's calibrator now *overshoots* the slope and should be re-evaluated rather than inherited, and retention is **not comparable across arms** because its denominator is that arm's own encoder. **Campaign remains on hold** pending the T06 decision and R11 |
| R7 | SEFL's net contribution unverified — **open**, decided by T10's A7 |
| R8 | ~~Reduced-epoch selection reverses gates with a training-free option~~ — **RESOLVED at T09**: `specs/09` §3 now carries the **training-free-option rule**, enforced by `TRAINING_FREE_OPTIONS` + an import-time check, merging `layout_mode` x `prior_mode` x `expr_mode` into one 18-cell full-budget gate. All six `cross-mix` cells rank bottom; **all three gates changed answer**, `layout_mode` included. New config `00ef4a19a2f576b8`; the donor-bar score goes **2/6 → 4/6** |
| R9 | ~~A gate decided on an unconverged incumbent~~ — **RESOLVED at T09**: `specs/09` §3's rule gained condition (2), *measured* per run — escalate every remaining gate when the incumbent falls by more than `selection_convergence_tol` on ≥2 metrics at the reduced budget. It fires here (shortfalls 0.36 / 0.43 / 0.81). Re-scored at 2400, `lookup` **still wins** (1.2 vs 1.8) — the winner does not flip back |
| R10 | ~~Scores reproducible only to ~0.012~~ — **RE-MEASURED ON REAL DATA 2026-08-27; the 0.0120 was a bug and the pooled 0.0335 is retired.** Same config, same seed, **separate process**: **36 of 36 values bitwise identical, largest difference exactly 0** (tier-1, both arms, means and per-fold). The original 0.0120 was `train/loso.py` and `infer/calibrate.py` seeding `np.random.default_rng` with the salted builtin `hash(section_id)` — fixed by `data.schema.section_seed`, so **every 'inside the envelope' verdict in this project was decided against a figure inflated by a defect**. **The replacement is per-metric and per-arm** (tier-1, `expr_mode`, seeds 2/3/4): morans **0.0574**, gearys **0.0595**, umap **0.0190**, marker-field **0.0148**, marker-depth **0.0472**, localization 0.0000 — a **4.0x range**, with the pooled figure too *lenient* on three and too *strict* on two. And **arm-dependent up to 22x** on the tier-1 `expr_mode` gate (0.0027–0.0084 vs 0.0148–0.0595), so a margin inherits nearly all its seed variance from one side. ⚠️ **Observation, not mechanism**: the obvious reading — a copying arm barely uses the fitted weights and is seed-invariant — **fails on the `deep_starmap` `text_emb_mode` gate**, where both arms are `zinb-flow` and **the worse arm alternates by metric** (`medcpt` on morans, `lookup` on the other four). The asymmetry is real and per-metric; its cause is unexplained, which is exactly why it must be measured per gate rather than reasoned about. Promoted to a **methods finding** in `specs/10` §4.2a — not reported in this literature, and it means a pooled envelope systematically flatters whichever comparison involves the steadier arm. **Verdict changes**: no overall verdict flips; `marker_field_r`'s envelope sub-test goes FAIL → pass (0.0206 beats its own 0.0148, was inside the pooled 0.0335) and it stays unestablished on the fold spread; `morans`/`gearys` weaken **3.9x/4.8x → 2.2x/2.2x**; `umap_mixing` strengthens to **7.7x**. **First claim in this project to satisfy `claim_min_seeds` = 3 on real data**: `cross-mix` beats `zinb-flow` on morans, gearys and umap_mixing. ⚠️ This envelope is **tier-1, `expr_mode`** — it may not be applied to `deep_starmap` or to `text_emb_mode`, which is the error it exists to retire. Two rules ship unchanged (`claim_min_seeds` = 3, `claim_tie_break_envelope` = 0.04). `reports/t09_envelope_starmap_seed{2,3,4}.md`, `specs/10` §4.2a |

## Numbers the paper needs

**[progress/numbers.md](progress/numbers.md)** — the per-task table, filled in as tasks land.

## Log

One file per task. Each records what was built, the measured numbers, and every
deviation from the spec with its reason.

| Task | Entry |
|---|---|
| T00 | [progress/t00_scaffolding.md](progress/t00_scaffolding.md) |
| T01 | [progress/t01_config_and_data.md](progress/t01_config_and_data.md) |
| T02 | [progress/t02_text_embeddings.md](progress/t02_text_embeddings.md) — includes the two `build_gene_meta` repairs and the B20 orthologue fallback |
| T03 | [progress/t03_noise_field.md](progress/t03_noise_field.md) — GATE 1, failed then re-run and passed |
| T04 | [progress/t04_field_and_retrieval.md](progress/t04_field_and_retrieval.md) — GATE 2, the escalation, and the per-query z window |
| T05 | [progress/t05_layout_head.md](progress/t05_layout_head.md) |
| T06 | [progress/t06_expression_head.md](progress/t06_expression_head.md) — includes the covariance demotion and C14's escalation |
| T07 | [progress/t07_sefl_losses.md](progress/t07_sefl_losses.md) |
| T08 | [progress/t08_metric_aware.md](progress/t08_metric_aware.md) |
| R8 | [reports/r8_budget_grid.md](reports/r8_budget_grid.md) — the 2 x 3 x 2 budget grid, the rank reversals, and the gap on record |
| R10 | [reports/envelope_synthetic.md](reports/envelope_synthetic.md) — the measured envelope and which gate choices fell inside it |
| T09 | [progress/t09_inference_and_calibration.md](progress/t09_inference_and_calibration.md) — the joint gate's four cells, the calibration statuses, R1/R3, and the two negative results |
| R11 | [progress/t05_layout_head.md](progress/t05_layout_head.md#t05-addendum--the-grid-multinomial-position-sampler-2026-08-24) — option D, the closed-form validation, and T05's re-measured numbers |
| R12 | ⚠️ **`decoder_mu_link` already defaults to `exp` (since 2026-08-21, commit `1968c09`), and every real-data audit is 2026-08-25 or later — so no negative in this project was fitted under `softplus` and no refit is owed.** What is open is narrower: the structured share has never been read off a *current* fit (15.1% is a tier-1 pilot under `softplus`; 61.4% is a caveated saved model). `scripts/t09_structured_share.py` reads it from the six existing `deep_starmap` checkpoints at no fit cost; `scripts/t09_checkpoint_config.py` settles the link from each checkpoint's stored `config_hash` rather than from dates |
| R12 | **Second open question, from T09 step 1**: per-gene `s` (the structured share of emitted count variance) spans **46-119x within a single cell** — `s_min` 0.0036-0.0093 against `s_max` up to 0.43. "The draw retains ~10%" is a median over a distribution two orders of magnitude wide, and **nothing measured says which genes are retained or why** (mean expression, detection rate, learned `theta` and real-tissue Moran's I are all unmeasured against `s`). A method that retains structure for the 30 genes an atlas is read on and loses it for the other 800 is a different method from one that loses 90% of everything, and every number reported so far is blind to the difference. `progress/t09_inference_and_calibration.md` |
| R4 | **Strongest instance to date, from T09 step 1**: `f_overdispersion = (1-pi) mu^2/theta` carries **57-61%** of the ZINB conditional variance — `medcpt` 0.6003 (0.5866-0.6140) against `lookup` 0.5961 (0.5677-0.6143). Two separate trainings, different gene embeddings, one working text channel and one `norm(0)` void, **same fraction in the same term**: the trade is a property of the decoder under this objective, not of one fit or one embedding. First time R4 is stated about a *named term with a measured share* rather than inferred from a divergence. ⚠️ Arm-independent is not dataset- or objective-independent; both arms are `deep_starmap` at one `decoder_mu_link`. `progress/risks.md` |
| R12 | **CLOSED 2026-09-01 as far as measurement can take it.** The finding is `log_ratio_spearman` = **+0.068**: over 1017 genes, how over-dispersed the decoder's `theta` head makes a gene has almost no relation to how over-dispersed that gene is within its own cell type (log-ratio IQR 1.891, a 6.6x quartile spread). **`theta` is not a dispersion estimate** — it absorbs mean variation the model failed to predict, gene-indiscriminately — which is why the whole moment-matching family of fixes is unavailable: there is no data-derived value to match to. Beside it: the per-cell freedom is real (`within_gene_sd_log` 0.418, a **1.52x** swing within one gene) and `f_overdispersion` **0.611/0.630** on a full-panel fit reproduces the six gene-split fits' 0.5677-0.6143, so the share is independent of embedding, text channel, gene split and panel width. The remaining gap is a **design decision** — the ZINB objective admits the trade and nothing in the loss opposes it. Two questions stay open: the 46-119x per-gene spread in `s`, and *why* `theta` is uncorrelated with the data's. `progress/t09_inference_and_calibration.md` |
| hazard | ⚠️ **Any new `Config` field invalidates every existing checkpoint's resume.** `content_hash()` is sha256 over every field and `require_compatible` refuses a mismatch, so adding one field with a default — even a no-op under that default — strands every checkpoint ever written. Adding `decoder_theta_mode` cost the **six zero-shot `deep_starmap` checkpoints, 23.5 core-hours** of resumability (no measurement lost; their JSONs were written). Not fixed: hashing only fit-affecting fields needs a classification that will drift, and its errors are *silent resumes of incompatible checkpoints*, which is worse than the problem. Mitigation is procedural — add fields in batches, re-run anything that must stay resumable first |
| replication | **PRE-REGISTERED IN TWO PARTS, both before any `cosmx_nsclc_3d` fit.** Part 1: `morans_pearson`, primary contrast **A2−A3** on held-out genes, SUPPORT / PARTIAL / REFUTATION / UNINFORMATIVE(a-d). Part 2 (new): the **seen/unseen sign flip**, stated on **A2−A3 on both gene pools** — the one contrast where neither arm is degenerate on either pool and whose held-out half is already Part 1's primary. FLIP REPLICATES needs both signs to reverse, both magnitudes to clear their **own pool's** envelope, and fold balance ≥ 0.25 everywhere; condition (e) adds a kept-pool envelope cap at 0.1322. ⚠️ The flip inherits `morans_pearson`'s selection problem entire, and its kept half is not an independent test — it has already reproduced three times. `progress/t09_inference_and_calibration.md` |
| A7 | **Costed 2026-09-01.** 3 seeds x 2 arms = 6 fits: **~6.2 core-hours on tier-1** (62 min/fit, 4.6x headroom) against **~23 on `deep_starmap`** (3.74-4.09 h/fit, 0.5x — saturated). Tier-1 is the right dataset by this project's own ceiling-first rule, subject to one free check: **does `L_thick` charge a non-zero loss there?** If not, A7 tests `L_prog` alone and must move to `deep_starmap`. **Better single spend than the replication — 13 % of its cost — but the dilemma is false at these prices**: run A7 first, then the replication |
| A7 | ⚠️ **BLOCKED at the binding check 2026-09-01; no fit ran.** `deep_starmap` is **ILL-POSED** — assumed thickness 42 µm against a minimum spacing of 40.6 µm, so slabs overlap and `L_thick`'s coarse-graining identity is false; the cause is the loader defaulting thickness to *median* spacing, which guarantees overlap on any non-uniform stack. Tier-1 is **MARGINAL** — 97 % of random planes refused, `smallest_error_noticed` never up to a 30 % error, though the draws that do fit charge strongly (1.70 at 30 %). Undetermined between an under-budgeted sampler (16 rounds needs acceptance ≥ 0.11) and starved geometry (`3h` is **0.75** of the z-extent on both, against **0.18** on the fixture). An acceptance probe settles it in minutes and is built. Neither `thickness_ratio` nor the thickness default is being changed to unblock it. `progress/t09_inference_and_calibration.md` |
| SEFL sampler | **`Config.sefl_rejection_max_rounds` 16 -> 96, 2026-09-01.** The old docstring's arithmetic was the bug: "four times the shortfall" makes `remaining` shrink by `(1-4a)`, so rounds needed scale as **1/a**, not O(1) — 17 rounds at a tenth acceptance, which 16 already missed. Measured on tier-1: acceptance **2.8-18.5 %** (median 3.7), **68** rounds needed at the worst plane, and **2 of 32** draws reaching `n`, so `L_thick` returned exact zeros on ~97 % of planes while the loss just read low. The geometry was never starved (`3h` is 0.75 of the z-extent on both real datasets against 0.18 on the fixture, where both constants were set). A budget, not a definition; invalidates nothing, since both call sites are unreachable at zero SEFL weight. ⚠️ **Declined**: an acceptance-adaptive proposal schedule, which would converge in 2-3 rounds at any `a` and is the first place to look if an A7 result turns on sampler behaviour. ⚠️ Two corrections in the record: T07's SEFL measurements were **not** taken on a broken sampler (fixture, acceptance 0.237, 100 % reach `n`), and `prog_terms` **does** use the same rejection loop, so `L_prog` was starved too and "A7 on `L_prog` alone" was never a viable fallback before this change |
| A7 | **Baseline arm only, seed 1 of 3 (2026-09-01).** Tier-1 SEFL-off: `morans_pearson` **+0.2340**, `gearys` +0.2357, `marker_depth_r` +0.6289, `celltype_localization` +0.7591 — far below tier-1's best available copy (0.713-0.784). Nothing about SEFL is readable until the `w_thick = w_prog = 0.2` arm exists. ⚠️ **Timing gate not satisfiable from the artifacts**: the fit has always been timed to the console and never persisted; `fit_seconds` now goes into the run JSON. ⚠️ **Reporting defect fixed**: the per-module table's `|diff|` column is a *per-gene mean of absolute differences*, not the difference of the two columns beside it — 14.4x apart on module 1, whose genes miss in both directions and cancel. It feeds the per-channel-group `ell` escalation (A2), so subtracting the columns under-states that case by an order of magnitude where it is strongest. ⚠️ Recorded but **not** a finding: detection calibration moves tier-1's autocorrelation metrics by ~**+0.10** where the project ships it off on two measurements, neither taken on tier-1 |
| replication | ⚠️ **STAGED GATE RUN ON cosmx 2026-09-01 — conditions pass, the text channel does not. No fits.** (a) room **0.9563** (deep: 0.9574), (b) held-out/kept ceiling **~1.00**, (d) `self` = 1.0 and the constant field degenerate by §4.2c's boolean test on all four rows — the instrument transferred to a new dataset and the shuffled floor is justified by measurement, not inherited. **But 163 of 191 held-out genes (85 %) are bare-symbol-only and the median descriptor is 6 characters**, against `deep_starmap`'s 0 bare symbols and 546 chars. A2 on a bare symbol is MedCPT encoding a string — `specs/10` §7's own "`lookup` in all but name" — and it would produce a **false SUPPORT**: an arbitrary-but-consistent per-gene vector beating a `norm(0)` void clears every criterion while supporting a far weaker claim. ⚠️ My pre-registration bounded only the *differential* coverage, which passes (13.6 % vs 12.0 %); absolute coverage had no threshold. **Cause**: `gene_meta.parquet` is 2 155 rows, all mouse, and cosmx is human — 121/960 symbols hit, and those that do resolve to mouse genes via case-insensitive matching. Blocked on a metadata fetch, not on 47 core-hours. Absolute-coverage and species thresholds now pre-registered before any rebuild |
| replication | ⚠️ **Human `gene_meta` rebuild resolved NOTHING — 0/960 full names. STOP on all three pre-registered thresholds, and the numbers are a property of the build, not the panel.** The accidental *mouse* table resolved 121/960 with 26 held-out summaries; the purpose-built human one resolved 0, which a correct human table cannot do. `n_in_table` 960/960 with `n_full_name` 0 is 960 **symbol-only rows**. Reproduced locally: with `mygene` absent, `build_gene_meta` warns, writes symbol-only rows and **exited 0** — a Convention 6 silent fallback, now guarded by `MIN_RESOLVED_FRACTION = 0.5` (exit 1 + the three causes named; `--offline` exempt; verified both ways). Leading cause, to confirm from the console: `mygene` is in the `extra` extras group, not `dev`, so `make install` does not provide the one sanctioned online step's dependency. **The dataset is good** — every scientific gate passed — and the thresholds stand unchanged for the next attempt |
| replication | **Human `gene_meta` resolves once `mygene` is installed** — 949/960 full names, 946/960 summaries (98.5 %), all **native** human, all `ENSG`, taxid 9606, confirming the missing-dependency hypothesis. ⚠️ Better than `deep_starmap`'s table, whose summaries are **83 % human-orthologue prose about a mouse gene**: cosmx's A2 would be a *cleaner* test than the original and the write-up must not conflate them. `t09_zeroshot_text_coverage.py` gains **`--species`**, required whenever `--gene-meta` is given (inferring it would be a Convention 6 silent fallback); mismatches are caught before `load_gene_meta` so the error names both arguments. The guard was right and is not relaxed. Coverage now also reports **`n_unresolved_in_table` per side** — a symbol the build could not look up (`HLA.A` for `HLA-A`; the panel writes a dot for a hyphen) is a symbol-only row that counts as "in the table" while carrying no text, and which side it lands on decides whether it is bare *in the arm under test*. Held-out count still to be measured; thresholds unchanged |
| A7 | **Seeds 1-2, both arms. The SEFL arm collapses the anatomical field, reproducibly.** `i_gen` **1.6 %** and **2.4 %** of target against the off arm's 97.5 %/95.6 %; both length-scale axes `target_unreachable` at 0 iterations; all four gene modules' generated Moran's I at zero to three decimals in both seeds. ⚠️ **Seed 1's apparent autocorrelation gain reversed on seed 2** (morans −0.052 → +0.280, gearys −0.067 → +0.307, signs differ) — the trap is now demonstrated, not argued: those metrics are correlations across genes, not amplitudes, and correlate noise on a dead field. Provisionally 5 of 7 metrics show SEFL costing with agreeing signs at **2.3x-5.6x** the worse arm's 2-seed spread; the two autocorrelation metrics are **not readable**. ⚠️ **First per-arm spread measured on tier-1: `morans` off-arm range 0.1839 = 5.5x the 0.0335 fixture envelope** the report footer quotes — a direct measurement of §4.2a's rule, with the worse arm alternating by metric. Separately (R1, not A7): the **calibrated** `ell` moves 1.86x between seeds while the **fitted** `ell` moves 7 %, so the bisection amplifies seed noise by an order of magnitude. Two seeds; `claim_min_seeds` = 3; arms confounded on `ell`. No verdict |
| A7 | ✅ **COMPLETE, 3 seeds x 2 arms — SEFL is HARMFUL on real tissue and ships at zero because it should.** Five of seven metrics show SEFL costing with signs agreeing **3/3** at **1.31x-4.68x** the worse arm's own spread (§4.2b): `umap_mixing` 4.68x, `marker_field_r` 3.76x, `gene_mean_spearman` 2.75x, `celltype_localization` 1.55x, `marker_depth_r` 1.31x. **The SEFL arm collapses the anatomical field on every seed** — `i_gen` at **1.35-2.38 %** of target against the off arm's 94-98 %, every module's generated Moran's I under 0.006, both length-scale axes `target_unreachable` at 0 iterations. ⚠️ The two autocorrelation metrics are **unreadable** (0.78x/0.83x, signs 2/3): `morans` deltas run **−0.052, +0.280, +0.577** because they are correlations across genes, not amplitudes, and on a dead field they correlate noise. A single-seed table would have led with those two. ⚠️ A7 was only possible after `sefl_rejection_max_rounds` 16→96; at 16 it would have reported "SEFL does nothing", a false negative invisible in every number. R6's scope corrected: flattening is a property of the whole consistency block, not `L_cross` alone. Arms confounded on `ell`; `w_thick` and `w_prog` tested jointly, not separately |
| replication | ✅ **STAGED GATE CLEARED 2026-09-01 — every pre-registered condition met, not yet launched.** Coverage on the human table: held-out summary rate **0.984** (>= 0.80), bare symbols **1.57 %** (<= 0.10), median descriptor **709 chars** (>= 200), differential gap **−0.0014**, species enforced by the script's own refusal. With the ceiling gate already passed (room 0.9563, ceiling ratio ~1.00, constant field degenerate) the run is cleared; both criteria were written before any cosmx number existed. **All 11 unresolved symbols are HLA loci mangled at source** (`HLA.A` for `HLA-A`), 3 held-out / 8 kept — inside threshold, but a coherent family whose spatial structure is likely strong on a tumour panel; a dot→hyphen repair recovers all 11 for minutes, and is a judgement call rather than a gate. ⚠️ **cosmx's text channel is *stronger* than `deep_starmap`'s** — 709 vs 546 chars and **0 % vs 83 % orthologue summaries** — so a REFUTATION there is the more informative outcome and a SUPPORT is weaker than it looks. ~47 core-hours; one timed fit before the other five |
| A7 follow-ups | **Six applied 2026-09-01.** (1) `repair_symbol` — dot→hyphen, general, consulted only for symbols the first lookup failed on, row filed under the panel's own spelling; recovers all 11 HLA symbols on cosmx. **Table not yet rebuilt.** (2) **Second collapse alarm**: `check_collapse` watches per-cell variance and is blind to spatial structure *by construction* — confirmed from code; `spatial_structure_ratio` + `check_spatial_collapse` + `Config.sefl_spatial_collapse_warn_fraction` now watch the axis that failed, threshold **0.05 deliberately blunt** because the only anchors come from a different construction and R12 warns a healthy model may carry just 15 % of real Moran's I; the ratio is logged unconditionally so it can be tightened from measurement. (3) **Timing gate enforced in code** — fit 1 writes `fit_timing.json`, every later fit refuses without it; verified on four paths. (4) The **fixture envelope is out of `t09_ship_starmap.py`'s footer**, replaced by tier-1's measured 0.0033–0.3687 spread. (5) `specs/10` **§4.2e** — A7's −0.0519/+0.2804/+0.5772 as the methods' worked example for `claim_min_seeds`. (6) `specs/10` — the replication's **asymmetry** recorded before the fits |
| replication | ✅ **ALL GATES CLEAR — launch-ready 2026-09-01.** After the dot→hyphen repair: **960/960** full names, held-out **191/191 summaries (100 %)**, **0** symbol-only, median descriptor **720** chars with a **minimum of 176** — no tail of effectively-bare genes under a healthy median. ⚠️ **A third asymmetry, cutting the same way as the other two**: the summary-rate gap now runs **+0.4 points in the held-out side's favour**, so the arm under test is slightly *advantaged*; with descriptors longer (720 vs 546) and native rather than orthologue prose (0 % vs 83 %), a REFUTATION is the stronger negative and a SUPPORT reads "replicated on a dataset whose text channel is richer". ⚠️ **`check_collapse` DID fire on A7 — 72-74 times per seed from step 250 — and no report read it.** My "did not fire and could not" was wrong; see the A7 row and `progress/t09_inference_and_calibration.md` |
| replication | ⚠️ **Fit 1 timed at 3.50 h — but it may have read the WRONG gene-metadata table, and that decides whether it counts.** `t09_zeroshot_run.py` had no `--gene-meta` flag, so the fit read `Config.gene_meta_path` — the **mouse** table — for a human panel. Nothing raises: `load_gene_meta` compares the table's rows to the requested species and both say mouse, so the pairing is silent and 121/960 symbols resolve by case-insensitive accident. **The coverage gate cannot catch this by construction** — it reads the table `--gene-meta` points *it* at while the fit reads the Config default, so the gate can pass on one table while the experiment runs on another. The run's `text channel:` banner settles it: `n_with_meta` near 960 = sound, near 121 = void. **Remaining five held until that line is read.** Fixed: `--gene-meta`/`--species` added (required together) and `check_text_channel` refuses below `MIN_PANEL_WITH_META = 0.5`, verified 960/960 and 1017/1017 allowed, 121/960 refused. **Cost model corrected**: 6 fits are **21.0 core-hours**, not 47 — at fixed `train_steps` the cost is steps-driven, not cell-driven (2x the cells, 0.94x the time), and my "cell count drives scaling" premise was structurally wrong |
| alarms | 🚨 **CORRECTION — `check_collapse` fired 218 times across A7's three SEFL-on fits and nothing surfaced it.** 72/74/72 firings per seed from **step 250** of 1200, `variance_ratio` settling at **0.105-0.193** against a 0.25 threshold. I reviewed those six fits across three rounds and concluded the alarm "did not fire and could not" — **both halves wrong**: it fired continuously, because in A7 the field lost per-cell variance *and* spatial structure together. The general orthogonality claim stands (a spatially-scrambled field with variance preserved exactly fires only the new alarm — a test builds it), so the spatial alarm is still justified, **by the orthogonality rather than by a miss**. ⚠️ **The real defect is worse than the one I diagnosed**: `TrainHistory.collapse_alarms` was populated and persisted into the resumable `.pt`, and **no report read it** — an alarm firing where nobody looks creates the impression of a watched run. `t09_ship_starmap.py` now prints the count to console and carries a **`## Collapse alarms`** section in the markdown report that leads with 🚨 and the sentence that every metric below it comes from a model that reported itself broken; a reused fit says "not measured", which is not "did not fire" |
| alarms | ✅ **Healthy `spatial_ratio` measured for the first time in the alarm's own construction — 0.05 stays, and the sign handling was wrong.** A real-data fit logged the ratio at **241 steps**: median **1.299**, and after step 100 (the warm-up `sefl_collapse_min_steps` already skips) **n=231, floor 0.5467, none below 0.05** — a **10.9x** margin on the floor, 26x on the median. First evidence the threshold is bounded above as well as below; it is **not** being moved, because retuning on one observation is the mistake already recorded twice here. ⚠️ **Right construction, wrong population**: the anchor is from a **SEFL-OFF** fit and the alarm only arms when SEFL is **on** — and the SEFL-on, running, not-collapsed population has **never been observed on real data** (every SEFL-on real fit is A7, and A7 collapsed). 0.547 bounds a healthy *decoder*, not the armed alarm; the docstring now says so. **Sign defect fixed**: four samples went negative (min **-0.2451**, all inside the warm-up, so nothing fired — latent, not harmless) and the alarm would have printed *"flat in SPACE"* about an **anti-correlated** field. Anti-correlated structure is not absent structure, and one message ranked -0.25 as a worse collapse than +0.01. `check_spatial_collapse` now returns `collapse`/`inversion`/`None` into separate `TrainHistory` lists and report lines. **The boundary is minus the threshold, not zero** — a spatially scrambled field (the textbook collapse, A7's shape) scores **-0.0133** because Moran's I under a permutation null is `-1/(n-1)`, so a zero boundary would file the canonical collapse as an inversion. No new `Config` field: `content_hash` unchanged at `445414903717491c`, so the re-run's checkpoint still resumes |
| scoring | ⚠️ **`t09_zeroshot_score.py` had the same gene-metadata hole as the run script — found by asking, before the six scorings.** It is **not** a pure consumer of checkpoints: `build_and_fit` (shared with the fitter) calls `build_entity_embeddings(cfg, ...)`, which reads `cfg.gene_meta_path`/`cfg.mygene_species` and encodes the panel's descriptors **before** `load_state_dict` overwrites them, and `ZeroShotView` embeds an unseen gene as `forward_zero_shot(base.text_vecs[g])` — so `text_vecs` **is** the zero-shot input, not an incidental buffer. `arm_config` took no metadata arguments, so every config was built at the **mouse** default. **Not the identical failure**: both fields are in `content_hash`, so the checkpoint's `require_compatible` would have refused — *after* a full MedCPT encode of the wrong descriptors, with a message naming a hash and no field. The run script's hole was silent; this one was **loud but unreadable**, and the six scoring commands could not have run at all. Fixed with the same shape: `--gene-meta`/`--species` **required together**, `check_text_channel` at the same `MIN_PANEL_WITH_META = 0.5` **before** `build_and_fit`, the docstring stating why the scorer touches text, and a test pinning all three. **Lesson, second instance:** a script sharing a builder with the fitter inherits every config field that builder reads — "it only scores" describes the intent, not the code path |
| replication | ✅ **RESOLVED — Part 1 PARTIAL, Part 2 DOES NOT REPLICATE, both under criteria fixed before the fits.** Seeds 2/3/4 on `cosmx_nsclc_3d`. **Part 1**: criteria 1, 3 and 4 pass (signs 3/3 seeds and 6/6 folds; A2 clears the `shuffled` floor at **2.08x**, against `deep_starmap`'s 2.52x; void condition 0.12x) and criterion 2 fails — **A2−A3 = +0.0450, 0.22x** the shared envelope, where deep had +0.2514 at 2.7x. A2−A4 meets 1–4 at **1.96x**, which is the PARTIAL branch verbatim: *text works, the route does not replicate*. **`specs/10` §7's mechanism sentence is withdrawn** ("the pure-text path is the contribution and the machinery around it is not" — a claim about A2 vs A3, undetectable here); **A2−A4 is kept**, so *text reaches a gene the model never saw* now has two positives on two datasets and *which path does it* has one. The asymmetry cuts right: cosmx's text channel is **richer**, so this is the stronger negative. **Part 2**: the flip's signs reversed on **12 of 12** seed×fold cells on both pools (held-out +0.0450, kept −0.0327, balances never below 0.39) and **both** magnitudes sit inside their pool's envelope (0.22x, 0.51x) → DOES NOT REPLICATE by criterion 3. ⚠️ **Its pre-registered reading is wrong and is corrected, not the verdict**: I wrote it as "a property of `deep_starmap`", assuming a magnitude failure would arrive with a sign failure. It did not. The direction replicated and the effect size did not. **Every gate closed**, including §4.2d's two constructions agreeing on every check — but **(c) clears by only 7.4 % under the per-fold construction** (0.2327 vs 0.2514), so the run is inside its stated power by a thin margin |
| §4.2g | ⚠️ **Not every arm's spread is an envelope — §4.2b's mirror failure, and it decided both replication verdicts.** On **both** gene pools the shared envelope was set by a **degenerate** member: held-out by **A4** (0.2015, the void arm, whose held-out genes all share one embedding so its `morans_pearson` is noise about zero), kept by **`shuffled`** (0.0646). No informative arm's variance entered either threshold — the four arms' own spreads are 0.0111–0.0318 and 0.0108–0.0134, so the bar sat **6–18x** above them and an effect consistent on 12 of 12 cells could not clear it. A4's *level* is what certifies the void condition and its *spread* is not a noise scale for anything; both are the same fact. §4.2b was written against the **steady** arm buying a verdict; this is the opposite. **Recorded as `specs/10` §4.2g and explicitly NOT applied to the run that exposed it** — a threshold moved after seeing where the data fell is the fifth-instance failure §4.2 already names |
| tooling | **The aggregator's hardcoded verdict is gone (2026-09-07).** `t09_zeroshot_aggregate.py` carried `deep_starmap`'s pre-registration inline — primary `marker_depth_r`, contrast A1−A3, §4.2a per-contrast envelopes — and printed a SUPPORT/REFUTATION reading over **any** seed files handed to it, under a title naming the wrong dataset. On the replication it produced a confident verdict for the wrong experiment. Now arithmetic only; the verdicts live beside their pre-registrations in `progress/`, and **`scripts/t09_zeroshot_envelopes.py`** applies the replication's — including §4.2d's two constructions, which nothing computed until it existed. Also new: **`t09_zeroshot_degeneracy.py`** (uninformative (d) on the **scorer's** constant field — the ceiling path was already established at the staged gate, the scorer's was not: bitwise row-identical, deviation exactly 0.0, CV separation **5.3e7x** against deep's 6.2e7x) and **`t09_zeroshot_pool_sparsity.py`** (the zero-total guess, pre-registered before the instrument was written) |
| zero-total guess | ❌ **RULED OUT on the pre-registered threshold, and the direction is backwards.** `cosmx` held-out zero-total **0.00146** (329/225 981) against a RULES-OUT cut of 0.05 — **34x below** it — and **0** on either kept pool, so `n_eff` = `n_cells` everywhere and the mechanism does not exist at a scale that could matter. More telling than the threshold: `cosmx` is **2.5x LESS** sparse than `deep_starmap` and **5.7x DENSER** per gene (detection 0.0933 vs 0.0164), so the dataset with the **5.6x smaller** effect is the one with more data per cell — sparsity predicts the opposite of what was seen. Per-gene sparsity dies the same way. ⚠️ **A hole in my own thresholds**: the "within 1.5x" clause is a two-sided band, so a ratio of 0.404 — refuting the guess *more* strongly than parity — returned false; clause 1 saved it, but a symmetric band for an asymmetric hypothesis would have read INCONCLUSIVE on a refutation. **Two things it did buy**: (i) A4's 0.2015 swing is an ordinary draw from a null correlation over 191 genes (E[range of 3] = 0.1228, z = +1.22; deep's 0.0930 is z = −0.41; the kept `shuffled` envelope lands within 6 % of theory) — so the two datasets' envelopes are **noise in the noise estimate** and **§4.2g is worse than recorded**: three seeds cannot estimate a threshold whose own CV exceeds 50 %; (ii) the verdicts are **robust** — A2−A3 is inside the envelope at 0.22x / 0.48x / 0.37x under the measured, the other dataset's and the theoretical envelope alike, so §4.2g's defect never decided either verdict. The 5.6x shrinkage remains **unexplained**, with tissue organisation, panel composition and text content the only surviving candidates |
| §4.2h + audit | ⚠️ **A two-sided band on a one-sided hypothesis — the sixth entry, and an audit found a second instance in a pre-registered criterion.** The sparsity guess's RULES OUT clause read "within 1.5x of the reference", a neighbourhood of parity `[0.67, 1.50]`, on a hypothesis predicting `cosmx` **sparser**. The measured 0.404 refuted it harder than parity would and the clause returned **false**; clause 1 fired and saved it, but a fraction above 0.05 would have left **no branch firing** and read a clean refutation as INCONCLUSIVE. 🚨 The audit found the same shape in the **zero-shot void condition** — "A4 sits *within* one envelope of the floor" — which detects a **leak**, and a leak can only push A4 **above** the floor. It did not bite (+0.0240, 0.12x, above and inside, so the one-sided form holds a fortiori) and is **fixed for future use, not applied retroactively**. Also found: `test_stack_coherence`'s copy-spike is documented as "within 0.10" and implemented correctly one-sided — prose defect, corrected. Audited clean: uninformative (b)'s 0.80x, A7's 0.90x, GATE 2's 0.90x, `sefl_consistency_ratio_warn`, fold balance (one-sided); the slope error, the coverage differential, `test_calibration_converges`, collapse/inversion (correctly two-sided) |
| §4.2i | ✅ **STANDING REQUIREMENT — quote the null's sampling distribution beside every measured spread.** Not a note on one result: **every envelope in this project is a three-seed estimate and none was ever reported as one.** For a correlation over `n` genes the null has `sd ≈ 1/sqrt(n-1)`, and the range of three draws — which is what a three-seed envelope *is* — has expectation `1.693 sd` and its own `sd = 0.888 sd`, **CV above 50 %**. All three measured envelopes are ordinary draws from that null (z = +1.22, −0.41, +0.11), so the 2.2x gap between the two datasets' envelopes is **noise in the noise estimate**, not a fact about the datasets. Any criterion of the form "the margin exceeds the envelope" is read against a random variable — true of **every claim in this project**, not only where it was checked. Does not license substituting theory for the measurement: the measurement is the criterion, the theory says how much to trust it |
| density | ⏸️ **Open candidate, deliberately NOT pursued.** `cosmx` detects the median gene in **9.3 %** of cells against `deep_starmap`'s **1.6 %** — 5.7x denser — and its held-out effect is 5.6x smaller. Fourth mechanism hunt of the campaign; three of the first three ended in "not this either" and the one that found something arrived after the fits it would have changed, so the base rate governs. **What would test it is recorded**: split the held-out pool by detection rate and re-score (no fits) — density compressing *contrasts* predicts a larger A2−A3 on the sparse half with arm *levels* roughly unchanged; a third dataset settles it. Two points cannot separate density from tissue organisation, panel composition, or tumour vs cortex. **Not evidence** |
| close-out amendment 1 | ✅ **Intersection consistency is the SECOND validated claim, promoted out of the residue.** Two crossing sections emit **bitwise identical** expression along their intersection on an **untrained** model with no consistency loss (`test_generation_is_intersection_consistent_by_construction`) — exact, at every checkpoint, for every model of this architecture, because the 3D noise field is continuous and every conditioning pathway is queried at **physical** points under the identity pose. It is a **stronger** claim than the loss written to earn it, and it **survived that loss being shown unnecessary AND harmful**: `L_cross` is vacuous in v25 and its residual minimiser destroys the field (per-gene variance **0.065** of real at `w_cross = 0.3` against **0.711** off), so `w_cross` ships at 0 — and the property was unaffected, because it never depended on it. **No competing method has it**: independent per-plane generation has no reason for crossing sections to agree. Belongs beside GATE 1 and GATE 2 as the third statement about the **representation**, and the only one that is exact rather than measured |
| close-out amendment 2 | ⚠️ **The budget objection is real but scoped to ONE negative, not four — the premise did not survive checking.** Measured budgets: **layout/R11 at 2400** (headline swap; count error confirmed at both), **zero-shot at 2400** (both campaigns; the script defaults to 2400), **metric-aware at both**, and **A7/SEFL at 1200 only** — `Config.train_steps`'s default, not T09's selected 2400. So the objection touches **A7 alone**, where the record already scoped it (*"SEFL adds nothing at the default budget of 1200"*, not "to the shipped model"), and the metric-aware result proves the objection is not hypothetical — the same comparison reverses between the two budgets. **It does not rescue A7**: that arm *collapsed*, `i_gen` at 1.6–2.4 % of target with 218 alarms from step 250 of 1200, and a budget ending after 950 steps of a dead field is not obviously one that escapes it. Cost to close: six fits at ~2x the per-fit cost, buying a scope extension on a three-seed negative, not a new result. **Claims table corrected**: "metric-aware NEGATIVE at the shipped budget" was written when the shipped budget was 1200 — at 2400 the weights **win** the selection (rank 1.0 vs 2.0) and **ship at 0.5**, but on margins of 0.0052/0.0101/0.0018, far inside R10's 0.0335 envelope, on one seed → now **UNRESOLVED at the shipped budget**, neither negative nor positive |
| marker_field_r boundary | ✅ **BOUNDARY ELIMINATED — done 2026-09-07 for zero fits and zero generation** (both sides were already on disk). On `resample-grid`, the **shipped** layout mode and the only density-comparable arm (spread **1.04x**), deficits below each section's own copy floor are **0.1729 / 0.1877 / 0.2043** and the boundary-vs-interior gap is **0.69x** the envelope. The weakness is **uniform along the stack**, so R3's regime and the boundary-clamp geometry are **out**. The other four arms read INVERTED and **must not be read**: condition (c) fires on all five, but the field modes emit **63.9x** the truth at `section_2` against **0.90x** at `section_6`, so the boundary is scored on a 64x-thinned, better-conditioned draw — the inversion is the count error through the metric. ⚠️ **The pointer at T05's intensity head is weakened, not confirmed**: that argument ran through the boundary regime, and `resample` does not use the intensity head to place cells at all yet still carries a **0.19** deficit — the weakness survives removing the layout head entirely, which points at the **expression path (R12)**, not T05. **Two defects in my own instrument, both found by running it**: (b) was `hybrid`'s reference applied to five arms (a category error, now per `(mode, sampler)`, and NOT EVALUABLE on grid); (c) was binary where it needed a magnitude — the 2.0x spread cut separating readable from unreadable is **post-hoc** and is labelled so beside the pre-registered binary result, never instead of it. §4.2h's failure in a new place |
| marker_field_r | ⚠️ **A weakness that survived three method generations — promoted to an OPEN ITEM (close-out §8.6), where it had no entry at all.** ⚠️ **The "four appearances" framing is WITHDRAWN (2026-09-07)** in `progress/numbers.md`, the instrument docstring and the close-out:  v25's single losing metric on the T09 fixture, the smoke run's 0.1611, and tier-1's **0.6384 against a `flanking_copy` floor of 0.8857 — 0.247 below, 7.4x the envelope**, worst of the six. ⚠️ **Stated at its real strength**: appearances 1–2 are a **cross-dataset pool**, which §4.2a forbids — per dataset it is **9–9**, on tier-1 v20 (0.8804) and v21 (0.8881) both **beat** SpatialZ (0.8522), and v20 wins **7 of 7** in the wide regime. Honest count: **two clean v25 appearances plus a pooled comparison our own methodology rejects**. **Why it is openable**: the deficit is **arrangement, not magnitude** (`gene_mean_spearman` 0.0033 off its copy floor, inside the envelope), and it is one of only two **pose-dependent** metrics with tier-1 rotations of 0.0–1.5° — so alignment is measurably not confounding it and a deficit at ~0° points at **T05's intensity head**. **First step costs zero fits**: alignment diagnostics already ship beside it, and stratifying existing tier-1 scores by distance to the boundary (R3's regime) is a scoring pass |
| alarms FIXED | ✅ **Both collapse alarms now arm independent of SEFL (2026-09-07).** They were gated on `sefl_teacher is not None`, and the teacher exists only when a SEFL weight exceeds zero — so on the **shipped** configuration, which has all four at zero, **neither alarm had ever run**. Every empty alarm record in every shipped campaign meant *never armed*; it was read as health at least twice, including by this project's own report generator and by the aggregator written to enforce §4.2f. Neither statistic is about SEFL: one reads per-gene variance of generated expression against real, the other their Moran's I ratio — both properties of the emission, which every configuration has. `_log_sefl` now arms on `step >= sefl_collapse_min_steps` alone, with `test_both_collapse_alarms_are_armed_with_sefl_off` pinning the gate from source (a fit that happens not to collapse cannot tell "armed and silent" from "never armed", which is the defect itself). Every place that cited an empty record as a check is corrected: the ship report, the A9 fit-1 entry, the A7 close-out's qualifier, and the aggregator |
| §4.2j | ⚠️ **An instrument that reports a check it did not perform — a new rule, and a different failure from the six before it.** §4.2a–d/g/h are about **criteria**; this is about the **artifact**. `t09_ship_starmap.py` printed *"Both were **checked** — a measured silence rather than an absent measurement"* on runs where the alarm was never armed, and `t10_a9_aggregate.py` — **written after §4.2f, to enforce §4.2f** — printed the same. Not §4.2f, which is a signal nobody reads: this is a report claiming it looked when it could not have, which is worse because it **terminates the enquiry** — a reader who sees "checked" stops checking. **The rule**: a report may state a check ran only if it can establish the check was **armed**, from the run's own record; the three outcomes *fired* / *armed and silent* / *never armed* must be distinguishable in the artifact; **never armed is not a pass**. Prefer arming a diagnostic unconditionally over reasoning about when it is relevant — the reasoning is where this went wrong |
| A9 | ⚠️ **UNINFORMATIVE — condition (a) fired, and the run found two other things.** Six fits, three seeds. The worst primary envelope is **0.4323** against the **0.067** (a) names — **6.5x over** — and both autocorrelation primaries have signs disagreeing across seeds. **The weights stay unestablished and a three-seed real-data design could not resolve them**, so "more seeds" is not a cheap path either. 🚨 **Finding 1: the collapse alarm is DISARMED whenever SEFL is off.** `train_ctfflow` gates it on `sefl_teacher is not None`, and the teacher is built only when a SEFL weight exceeds zero — the shipped config has all four at **zero**, so **on the shipped model `check_collapse` never runs** and every empty alarm list in every SEFL-off campaign means *never armed*, not *did not fire*. My own aggregator printed the wrong one of those two on its first run: §4.2f failing inside the instrument written to enforce it. Owed fix in the model code. 🚨 **Finding 2: applied by hand, it would have fired on all three ON fits** — median `variance_ratio` **0.86/0.77/0.81** off against **0.14/0.08/0.17** on, threshold 0.25, where A7's *collapsed* arm sat at 0.105–0.193. And `spatial_ratio` goes **up** (1.4–1.6 → 2.2–2.5) as variance drains: the autocorrelation term hits its target statistic while the amplitude leaves — **R4's shape in a fifth place, candidate not established**. Wall clock **1.63x** (93 vs 57 min). Report: [reports/t10_a9.md](reports/t10_a9.md) |
| A9 pre-registration | 📋 **Kept for the record, 2026-09-07, not yet run — the metric-aware six fits.** `(2400, on)` at the shipped 0.5/0.5/0.5 against `(2400, off)`, three seeds, tier-1, `layout_mode=resample`, pinned evaluator. A **removal** experiment, unlike A7's addition. Primary metrics are the three the losses are **built from** (`morans_pearson`, `gearys_pearson`, `marker_depth_r`); `celltype_localization` is inert under `resample` and **excluded, not scored as a tie**. **POSITIVE** needs two of three primary margins positive with signs at every seed and fold, clearing the run's own shared envelope under **both** §4.2d constructions, and no primary clearing negative. **NULL** — every primary inside the envelope — is written as explicitly and is the likelier outcome: it means the 0.5 selection was a coin-flip on a rank, that **every absolute number in the project was produced with three inert terms active**, and that the right disposition is to **say so rather than turn them off** (turning them off post-hoc re-opens every fitted number for a change within noise). NEGATIVE would require re-opening the absolute numbers, and that cost is named in advance. Four uninformative conditions, including **(d) any collapse alarm firing** — A7's lesson. **No outcome changes a close-out verdict**: R11, A7 and the zero-shot results are within-configuration contrasts carrying the same weights on both sides. Driver: `t09_ship_starmap.py --w-metric-aware`, all three moved together because `specs/09` §3 makes them one gate; timing gate imported from the zero-shot driver, all four paths verified |
| checkpoint compatibility | 🚨 **Adding a buffer stranded every earlier checkpoint — the `content_hash` hazard's HARD form, found only when something finally tried to reuse one.** The `umap_mixing` re-score died on `runs/pilot/model_exp_2400.pt` with *Missing key(s) in state_dict: `decoder.gene_theta`*. That buffer arrived with `Config.decoder_theta_mode` (2026-09-01), registered unconditionally under a comment claiming it made checkpoints interchangeable — **wrong in both directions**: earlier fits lack the key, and a moment-matched table loaded into a `learned` decoder would size-mismatch. ⚠️ A `content_hash` change refuses a *resume* and can be worked around; a **state_dict** key cannot be worked around at all — the weights are there, they are correct, and nothing loads them. That model is the fit behind `reports/pilot.md` §13 and `r11_starmap_layout_modes.json`, **unloadable for a week** with nobody noticing. **Fixed from what the buffer IS**: under `learned` nothing reads it, so a missing key is filled and a stray `(G,)` table is dropped; under `moment_matched` it **is** the emitted dispersion, so its absence **raises** with the reason — `strict=False` would have loaded a zero-width dispersion into a decoder that reads it, the silent fallback Convention 6 forbids. `test_a_checkpoint_written_before_gene_theta_existed_still_loads` pins all three. **Rule earned**: a new buffer is a breaking change to every existing checkpoint and announces nothing — `Config` fields at least surface through `content_hash` — so any future buffer needs its `_load_from_state_dict` path decided when it is added, distinguishing a buffer that is **read** from one merely **present** |
| artifacts landed | ✅ **24 untracked artifacts committed 2026-09-08 — they were untracked, not missing.** A `git status` on the campaign machine found **45** files no one had ever added. Landed: the **three-seed real-data envelope** (`t09_envelope_starmap_seed{2,3,4}` + a `_dup`), the ceiling bootstraps, `deep_starmap`'s zero-shot ceilings and gene split, the `text_emb_mode` envelope at three seeds, and **`resources/cosmx_panel_symbols.txt`** (959 symbols) — the input that unblocks rebuilding the cosmx gene-metadata table. ✅ **`specs/10` §4.2a's table is now sourced AND verified**: recomputing the across-seed spread from the committed files reproduces every cell exactly (0.0054/0.0574, 0.0027/0.0595, 0.0068/0.0190, 0.0049/0.0148, 0.0084/0.0472). ⚠️ **And that exposes the 0.0335**: it is sourced (`envelope_synthetic.md`, R10) but is a **pooled figure measured on the SYNTHETIC FIXTURE**, while the real-data per-metric envelopes span **0.0148–0.0595**, a 4.0x range straddling it. §4.2a states that a pooled envelope errs both ways; the practice throughout — the advisor report included — divides by the pooled fixture number anyway, so every "Nx the envelope" means N times a synthetic pooled figure. Redoing them per-metric needs no scoring and is the cheapest outstanding correction. Still uncommitted: the 21 `logs_*.txt` |
| provenance | ⚠️ **§8 split into three tiers, and the second one is the finding.** **8a HELD LOCALLY, NOT COMMITTED** — A9's six inputs, A7's six plus the binding check, the three cosmx seeds, the cosmx split/ceilings/coverage/degeneracy, both pool-sparsity reports and `gene_meta.cosmx_human.parquet`: measured, files exist, destination paths named. ⚠️ **Two attempts to land them failed** — a 403 on the campaign machine's push, then a `find` over the checkout matching **none** of them, so they are not under the repo working tree at all. The heading said "being committed" and is **corrected**: that was a claim about the future, which is what §8c says not to do. Until they land, every verdict citing them is attested by a progress entry rather than an artifact — §8b's status, differing only in being expected to be fixable. **8b MEASURED BUT UNRECOVERABLE** — R12/R4's supporting figures (`mu` 0.861, `theta` 61–63 %, **Spearman 0.068**) and the T09 selection table that put the weights at 0.5. No file is held; they are attested by a progress entry alone. ⚠️ The chain figures beside them **are** sourced (`reports/chain_2400*.md`), so R12's **localisation** to the count draw survives — what lacks an artifact is the **attribution to `theta`**, which is the paper's strongest mechanism claim. The selection table is cheap to regenerate (~8 min a fit on the fixture) but a regeneration is a **new measurement, not the one that made the decision**. **8c**: a headline result attested only by a log is the same class as §4.2f, sharpened by §4.2j — the rule for the next campaign is that the artifact is committed in the same change as the claim, or the claim is labelled attested-by-log; "we still have it locally" is not a third state |
| advisor report | 📄 **[reports/advisor_report.md](reports/advisor_report.md)** — regenerated 2026-09-07 from the amended close-out and the record, not from the earlier draft, which predated A9, the `marker_field_r` correction and the alarm finding. Carries the three additions: **A9 UNINFORMATIVE** with §4's standing recommendation to zero the weights and its four reasons; the **collapse alarm never having been armed on the shipped configuration**, so empty alarm lists in SEFL-off campaigns meant *never armed* rather than clean; and `marker_field_r` at its **real strength** — two clean v25 appearances plus a cross-dataset pool §4.2a forbids, not the withdrawn four. **Every number names its file**, the six-metric table is rebuilt on the **shipped** `resample-grid` arm rather than the superseded `hybrid` pilot row (and `umap_mixing` is reported as **NaN/no current measurement** rather than back-filled from it), and §8 splits provenance into **8a recoverable / 8b measured-but-unrecoverable / 8c why it matters**. ⚠️ **§5 now states the draft-to-draft change explicitly** — `marker_field_r` 0.6384 → **0.6830**, `marker_depth_r` 0.7478 → **0.8554**, because the earlier draft used the superseded `hybrid` pilot arm — so anywhere "0.247 below the floor" is quoted it describes a **layout mode that no longer ships**; the shipped deficit is **0.203**. And `umap_mixing` is stated as a **hole in the shipped configuration's own characterisation**: NaN on **all five** r11 arms, so `--no-umap` was passed to that whole campaign and one of the six headline metrics has **never been measured on the shipped layout mode under the current sampler**. Costed: one generation-and-scoring pass on `runs/pilot/model_exp_2400.pt`, no fit — ⚠️ duration not measurable from the artifacts, since `fit_seconds` times the fit alone; ~1 h if the checkpoint is gone and it becomes a refit |
| T09 close-out | 📄 **[reports/t09_closeout.md](reports/t09_closeout.md)** — the consolidated statement, written 2026-09-07 after the replication, A7 and the sparsity test. Nothing pending measurement. Headline: **a continuous 3D field is a good representation of a tissue volume and a bad generator from it** — GATE 2 at 95.5 %, controllable autocorrelation, **exact** intersection consistency, and text placing unseen genes above a floor on two datasets; against which every generative component loses to **copying**, including the SEFL losses the method is named for. Records the sentence nobody had written: **v25's shipped configuration is beaten by v20's fallback on every live metric**, since the two differ exactly by v25's contribution. Failure localised to the ZINB objective closing an amplitude gap with dispersion (`theta` at 61–63 % of conditional variance, Spearman **0.068** with the data's own). Nine methods rules, with the discount stated: **we found these by getting them wrong first** |
| metric-aware | 🚨 **The one component that ships ON while established by NOTHING — flagged, not buried.** `w_autocorr = w_profile = w_distribution = 0.5` ship on because they won a **one-seed aggregate-rank** selection at 2400 (rank 1.0 vs 2.0, four of six metrics) after *losing* at 1200 (rank 3.5 vs 3.0). The per-metric margins at 2400 are **0.0052 / 0.0101 / 0.0018** against R10's **0.0335** envelope — inside it by 3x to 19x. The selection is sound as a selection and is **not evidence the losses do what they are named for**. **A different kind of problem from the rest of the negative column**: a component shipping *off* with evidence against it is an honest negative the shipped model does not depend on; one shipping *on* while established by nothing sits **inside the baseline every number in this project was measured against** — a claim the paper's setup makes silently rather than one the paper makes. Does not weaken the negatives (each is a within-configuration contrast with the same weights both sides); the exposure is to the **absolute** numbers and to "best configuration found". Resolving it is **six fits** — `(2400, on)` vs `(2400, off)` at three seeds — the cheapest unresolved item and the only small spend touching the shipped baseline |
| T09 FINAL close-out | [progress/t09_inference_and_calibration.md](progress/t09_inference_and_calibration.md) — supersedes the earlier one (four of its five open items resolved or withdrawn; its headline carried a `decoder_mu_link` claim its own addendum corrects). Assessment unchanged and better supported: **negative results plus a methods contribution, the methods half stronger** — but the largest negative is now a named, reproduced mechanism finding rather than an unexplained collapse. Open: R4's ZINB trade (a design change, not a measurement), R12's two questions, A7 (SEFL ships at zero weight and is in the title), R14's donor rule. **Recommendation on the last live decision: spend the `morans_pearson` replication** — the 2.52x is worth ~0 unreplicated, a pre-registered second dataset is the correct remedy for a post-hoc metric, and it tests the seen/unseen sign flip at no extra cost |
| emission repair options | 📄 **[reports/emission_repair_options.md](reports/emission_repair_options.md)** — 2026-09-09, design comparison, **nothing built**. 🚨 **The brief's premise does not survive checking**: `reports/chain_2400.md` is the **superseded `softplus`** arm (`t10_chain_diagnostic.py` carries `--decoder-mu-link`, `Config` has defaulted to `exp` since 2026-08-21, and the record states the swap as +0.1297 → **+0.4782**). On the shipped link the count draw reads **0.9008 → 0.5253** and **0.9098 → 0.5408**, retention **60.5/61.0 %** against tissue's 79.8/81.1 %, with generated counts *more* autocorrelated than the real section's. 🚨 And **"0.09–0.19 vs 0.62" is a cross-panel comparison** — `deep_starmap`'s 1017 mostly-unstructured genes against tier-1's 28 marker genes — which `progress/` already names as an error *"applies to the record's numbers too"*. **The panel-matched failure that does exist**: `deep_starmap` top-32, retention **25–34 %**; tier-1 at exp shows ~103 % but must not be quoted as a shipped-decoder property; **no measurement separates dataset from saved-model artifact**. ⚠️ Every chain artifact also runs at `expr_pca_dim=16`, the pilot stand-in. **An arithmetic bound rules out two candidates from data already held**: with R4 (v)'s decomposition, removing overdispersion reaches s′ 0.19–0.36 and **pure Poisson 0.24–0.43**, while 0.62 needs `f` **2.2–5.6× below the Poisson floor** — so **A (theta prior/floor) and C (Poisson + floor) cannot be sufficient**, and `Var(mu)` must rise **2.2–5.1×**, which the independent `sd(log mu)` 0.777-vs-1.213 ratio (2.44×) corroborates. Candidates costed A–D plus three the brief omits: **E** raise `Var(mu)` (the binding constraint), **F** emit the conditional mean (a framing decision, not a repair), **G** replace the likelihood with a proper scoring rule (the only one aimed at R4's root). ⚠️ **B and D each carry A9's gaming surface** — B penalises a variance-normalised ratio, D breaks the independence the retention statistic is defined under — both need the gaming outcome pre-registered as expected. **Order: two measurements first** (re-run the chain on the shipped config; measure `Var(mu)` per gene), then A as a cheap test of the diagnosis, B as the first candidate fix. **Honest view**: no single candidate closes the real gap; the fix is a combination (bound dispersion **and** raise `Var(mu)`), i.e. a T06 redesign |
| v20/v21 audit + honesty | 📄 **[reports/v20_v21_evidence_audit.md](reports/v20_v21_evidence_audit.md)** — 2026-09-09, the v20/v21 evidence base held to the conventions v25's claims were withdrawn under. 🚨 **The prior-campaign `per_section_metrics.csv` has never been in this repository** (working tree + `--all --reflog`), so `specs/10` §13.2–13.4 — the entire v20/v21 case — is **attested by the spec document alone**: §8b for the project's new headline. **Survives**: the instrument (the re-score closed it for tier-1), the methods themselves (runnable, **no `wrapper_args` on either**, self-describing via `method_params`), §13.3's already-caught pooling caveat, and the *direction* of every signal. **Unestablished**: every **margin** — the prior campaign has **zero seed replication**, so no v20/v21/SpatialZ margin is a claim under §4.2a/b, and v20/v21's determinism has never been measured at all; every **level** — §13 quotes **no floor and no ceiling**; every **number** — until the re-scored tree replaces the prose. **Defects found**: §13.2's header says *cell-count-weighted* where §13's preamble says median (numbers are the medians — 0.9704 is §4.6's median, not its 0.8970 mean — so the **label** is wrong, on the one metric §4.6 uses to show the estimators invert the verdict); §13 reports **7 metrics** and never names which seven; and the v20/v21 **numpy fallback screening is a run-time log check that `evaluate_all` cannot re-perform**, so the re-score inherits it. **Not fixable by measurement**: **fourteen** internal versions are registered and bench3's own notes say *"benchmark-driven fixes"* (v18) and *"recalibrated on the real benchmark"* (v22) — v20/v21 were selected on the protocol they would be reported on. **Cost**: four of six remediation items are reading only; the two that cost runs are **9 comparator runs at tier-1** (three seeds of v20/v21/SpatialZ) and **9 at `wide_3_4_5`**, which also answers C2. ⚠️ No comparator timing exists in this repo — time one before committing to eighteen. **Honesty verdict**: the framing is sound and not yet honestly evidenced; the sharpest correction is that **v20 is itself a structured copier** (R13: `cross-mix` under `resample` *is* a copy), so the claim is *"the winning design is a copier on a task where copying reaches 81–98 % of achievable, and v25's negative explains why"* — dishonest only if reported without the copy floor beside it. The one thing that would make it dishonest: **spending v25's audit as credibility while exempting v20/v21 from the audit** |
| re-score + envelope propagated | ✅ **2026-09-09.** **(1) The comparator re-score is a scoring pass, not a campaign** — predictions were kept, so `evaluate_all --force` closes §13.1's cross-instrument defect with **no method re-runs**; command, checks and cost in `specs/10` **§13.1a**. ✅ The §0 pin verified in this checkout (764 lines, `73626692…9538992`). 🚨 **Copy the results tree first**: `evaluate_prediction` writes `metrics.json` **in place**, and the old values are the only record of what the older evaluator revisions returned — §13.1 calls the drift on shared metrics *unknown and undeterminable from the CSV*, and re-scoring into a copy is the one chance to measure it. ⚠️ **No per-prediction scoring time exists anywhere in the corpus**, so the cost is not modelled: run `--methods spatialz` first, time it, multiply (§11's discipline). Keep UMAP on; require `failed 0`. Payoff beyond comparability: `field_ssim` / `gene_detection_spearman` / `rare_celltype_localization` are 132/132 for v18/v20/v21 and **0/3** for spatialz on tier-1, and one pass populates every column for every method. **(2) A9's `w=0` spreads propagated** as the only envelope measured on the shipped configuration (`reports/envelope_correction.md` §5b): substitutable for **one** family — the shipped table's deficits below the copy floor — giving the **first admissible envelope multiples in the project**: morans **21.1x**, gearys **17.8x**, marker_field **10.4x**, marker_depth **2.1x**. The pooled figure was mis-scaling in **both** directions (autocorrelation pair 12.7x/12.8x → 21.1x/17.8x; marker_depth 7.7x → 2.1x). Everything else stays on its own envelope or stays flagged: §2.1/§2.2 are instrument A, §2.3/§2.4 are the orphan `w=0.5` fixture arm, §3's six rows are r11's `lookup`/`pca16` arm, A7 keeps its 1200-step spreads. 🚩 **`celltype_localization` NOT READABLE** — its `w=0` spread is 0.0009 with **bitwise-identical `n_pred` across all three seeds** (resample copies the layout), so an 18.9x multiple is manufactured from an arm that barely moves: §4.2g's degeneracy arriving on **the arm under test** rather than on a referent, which the rule does not cover. ⚠️ **Correction to the same day's earlier entry**: `gene_mean_spearman`'s deficit is **0.4x its own envelope — a tie**, so "flips sign against its floor" overstates it; still withdrawn as *solved*, not established as *worse* |
| 0.5 has no source | 🚨 **2026-09-09 — the seventh provenance failure mode, and neither pre-registered branch.** `runs/select/starmap_visual_cortex/selected.yaml` **does** exist (added `3d57725`, deleted `5cd1fd6`, still in history) — and it carries the three weights at **0.0**, with `train_steps: 20`, `decoder_mu_link: softplus`, `expr_pca_dim: 32`: a 20-step smoke artifact from the halted pilot, predating the link fix, the clamp rule and R11. **So `0.5` appears in NO machine-readable artifact this project produced** — not `Config`, not any fit's recorded config, not the one persisted selection. Only prose, in documents citing each other. **Stronger than "never persisted"**: §8c's six modes are all about a *file*, and this one has none at its centre — nothing is missing because nothing was ever written, every artifact is internally consistent and every document is wrong, and no artifact-to-artifact check can catch it because they all agree at 0.0. Written as `specs/10` **§4.2a-iv**: *a value the record calls shipped must be traceable to a persisted config, a recorded fit config, or a declared default in code.* ⚠️ **The instrument returned a false negative first** — `t09_find_selection.py` walked filesystem roots while the file was missing in *time*, inside the repo it ran in, and quoted §8c's reflog lesson in its own output while doing it. §4.2j, second time in this thread. Fixed with a `--all --reflog` history scan; **the self-check now regresses on the false negative itself**. **Consequences:** (1) 🚩 **the standing recommendation is WITHDRAWN as vacuous** — it recommends changing a state that does not exist, and its first reason (*"selected on the fixture"*) rests on the §8b-unrecoverable table, so **even "a selection chose 0.5" is prose-only**; what replaces it is documentary. 🚨 **A9 was framed backwards** — with 0.5 unsourced it is an **ADDITION** experiment (the A2/A4/A7 inversion again), its **`0` arm the shipped configuration** and its `05` arm the addition; the UNINFORMATIVE verdict is untouched. (2) **R10's envelope was measured on an arm with no other instance in the project** — `t09_envelope.py` sets all three to 0.5 for its nine fits, so the 0.0335 and its per-metric decomposition describe a configuration appearing nowhere else: not "the wrong arm" but **an arm with a single instance, created by a script on the strength of a value with no source**. ✅ Unaffected and now the figure to quote: **A9's `0` arm is the shipped configuration**, so its spreads (morans 0.0202, gearys 0.0241, umap 0.0464, marker_field 0.0307, marker_depth 0.1225, ct_loc 0.0009, gene_mean 0.0400) are **the only envelope ever measured on what the project actually ships** |
| shipped config measured | ✅🚨 **2026-09-09 — the headline table never measured the shipped configuration, and the measurement already existed.** Both drafts are **ablation A3** (`lookup` + `expr_pca_dim=16`; the 2026-09-08 fix moved the layout gate and inherited the label on the two not in dispute). Stated as an **absence**, not a caveat, in advisor §5.0. **The remedy is A9 again**: its six fits are the shipped configuration on every gate but the disputed weights — `medcpt`, pca 28, `resample`+`grid`, 2400 steps, 3 seeds, pinned evaluator — so the table costs **zero fits** (2.85 + 4.65 core-hours, already spent). Probes transfer because they are model-free. 🚨 **The shipped arm is WORSE than the arm labelled as it on six of seven metrics**: morans 0.6541→**0.5574**, gearys 0.6535→**0.5543**, umap 0.9262→**0.8318**, marker_field 0.6824→**0.5655**, marker_depth 0.8331→**0.7228**, gene_mean 0.9901→**0.9721**; ct_loc +0.005 (inside its 0.0061 envelope, a tie). **Every deficit below the copy floor grows** — marker_field 0.203→**0.320**, the autocorrelation pair ~0.33→**~0.43** — so the negative column is stronger on the real method than on the stand-in. 🚨 **`gene_mean_spearman` flips sign against its floor** (+0.0038 → **−0.0142**): the *"one genuinely solved thing"* is **withdrawn**. What a headline table still lacks is the **comparator set**, which §3 already says is not in this repo and §12 already budgets — not more v25 fits. ⏳ **PENDING**: `scripts/t09_find_selection.py` (new, read-only, prints the roots it searched) settles whether a `selected.yaml` carrying 0.5 exists. Both branches pre-registered in advisor §6b — **(a)** it exists → selected, persisted, applied by no run: a wiring gap; **(b)** it does not → 🚨 **a seventh provenance failure mode, a value chosen, recorded as shipped, and never run**, distinct from all six in §8c because nothing is missing or mislabelled and every artifact is internally consistent while the claim is false |
| recovery ran | 🚨 **2026-09-09 — the arms do NOT match, and it found two things it was not looking for.** `t09_recover_checkpoint_config.py` was run; the five r11 artifacts now carry a `recovered_config` block. The pilot checkpoint is **`text_emb_mode=lookup`, `expr_pca_dim=16`, metric-aware `0/0/0`, fitted `layout_mode=field`**. (1) **It closes NONE of the six flagged figures** — A9 is `medcpt`/28, so the arms differ on two **fit-time** gates; they agree only on the weights, which was the gate the flag was about. My prediction that it would close four was wrong, and the way it was wrong is §4.2a's own failure mode committed while writing the correction for it: **I checked the gate in question and never asked what else would have to agree.** What it did settle is the *reason* — from "no candidate donor known" to "the one candidate is measurably the wrong arm", with a costed remedy. ✅ It also made R11's **ordering** firmer: all five arms are provably one fit differing only in generation-time gates, and `layout_mode` is in `FIT_INVARIANT_GATES` (bitwise across 96 tensors). (2) 🚨 **The six-metric "v25 shipped" column is MISLABELLED.** `layout_mode` is sound (generation-time, fit-invariant, so it really does generate under `resample`), but **`text_emb_mode=lookup` is ablation A3** — `specs/10` §3 says so in as many words — and **`expr_pca_dim=16`** is the pilot stand-in the clamp rule replaced with 28, both fit-time and not overridable. **The same failure the table was corrected FOR**: the 2026-09-08 revision fixed the layout label and left the expression labels wrong, because only the layout gate was in question. (3) 🚨 **"Every absolute number was produced with the metric-aware weights active" is BACKWARDS.** `Config` declares all three at **0.0**, `base_config` never overrides, `--w-metric-aware` defaults to unset — the r11 checkpoint, A7's two arms and A9's `0` arm are all at 0/0/0, and **A9's `05` arm is the only run in the corpus at 0.5**. The absolute numbers are *cleaner* than claimed, not dirtier; the defect is that they are not the configuration the record calls shipped — a chosen value nothing implements. ⚠️ And `t09_envelope.py` sets the weights to **0.5** for its nine fixture fits, so R10's 0.0335 was measured on the weights-**on** arm while every number it judged was weights-**off** — a fourth axis of mismatch. ⚠️ Not checkable here: whether a `selected.yaml` carrying 0.5 exists on the campaign machine; no run whose artifact is in this repo resolved one. **Rule earned** (`specs/10` §4.2a-ii): *a table may be labelled with a configuration only when every fit-time gate has been read out of the artifact — not just the gate under test* |
| envelope correction | ✅ **The 0.0335 correction — every clearance figure re-read against its own envelope (2026-09-08).** Zero fits. [reports/envelope_correction.md](reports/envelope_correction.md). **Nothing in the negative column moves** (every negative is a within-configuration contrast, so the divisor changes the scale and not the sign) but the multiples do: `expr_mode` tier-1 **4.6–5.3x → 2.2x / 2.3x / 7.4x**, tier-1 headroom **4.6x → 3.3x**, deep headroom **0.5x → 0.37x** and **2.6x → 2.0x**, R14's donor rule **3.5x → 2.7x**, the metric-aware margins inside by **3–19x → 1.6–19x**. Note the direction: on `expr_mode` the error **overstated** the two autocorrelation metrics by >2x and **understated** `umap_mixing` — §4.2a's own claim arriving in this project's headline numbers. 🚨 **Two verdicts change.** (1) The fixture `layout_mode` gate was **NOT decided inside the noise**: per metric `umap_mixing` clears at **3.49x** toward `resample` and `celltype_localization` at **2.84x** toward `hybrid`, both 3/3 on sign — the fixture saw two answers that disagreed, so *"underpowered, not wrong"* is withdrawn. (2) **BOUNDARY ELIMINATED → NOT READABLE**: its instrument hard-codes `envelope: 0.0335`, and the two candidate divisors **disagree** — the −0.0231 gap is **1.56x** (outside) against instrument A's `marker_field_r` 0.0148 and **0.39x** (inside) against A9's instrument-B 0.0596, with neither admissible. A verdict that flips with the divisor is not a verdict; the raw finding stands (the boundary section has the **smallest** deficit). 🚨 **Unanticipated finding: the corpus has two instruments and every envelope is on the wrong one** — `section_scores` on internal LOSO vs `bench3.evaluate_paper` on `paper_2_4_6`, which §5 forbids mixing, differing **2.6x–6.7x** per metric on tier-1. ✅ **The missing envelope existed inside A9** (`t10_a9_{0,05}_s{1,2,3}`: 3 seeds, 2 arms, 2400 steps, pinned evaluator, spreads **0.0009–0.2894**, and it carries `paper_gene_mean_spearman`). 🚩 **Six figures flagged, not substituted**; five share one blocker that is **not compute** — `runs/pilot/model_exp_2400.pt`'s config is in none of the artifacts citing it (no `config_hash`, no `text_emb_mode`, no metric-aware weights), so A9's envelope cannot be matched to the arm behind the six-metric table. Two rules earned: **§4.2a-i** (an envelope is per *instrument*; with no match, report the raw margin and say the multiple is unavailable) and **§4.2a-ii** (an artifact records the arm it describes). `specs/10` §4.2* now names **nine** choices, not seven |
| envelope correction: follow-ups | ✅ **2026-09-08, same day.** (1) 🚩→✅ **The r11 config was never lost**, and my earlier "cannot be identified" is corrected: `t10_rescore_saved.py` writes and reads the model file as `{"config": <every Config field>, "state_dict": ...}` and already prints five gates from it — the whole `Config` has been in `runs/pilot/model_exp_2400.pt` throughout, and the defect is that the *reporting* scripts never copied it out. New `scripts/t09_recover_checkpoint_config.py` reads both payload shapes (saved fit = direct reads; resumable `FitCheckpoint` = gates *inferred* from `history.terms` and `teacher`, labelled as inference) and `--patch`es a `recovered_config` block into named artifacts. ⚠️ Not run here (no `torch`/`numpy` in this container); `--self-check` passes on both shapes and both error paths, and the patcher was verified against a copy of `r11_determinism_a.json` — idempotent, refuses overwrite, every original field byte-equal. (2) **Both hard-coded divisors closed**: `t10_marker_field_boundary.py` now *requires* `--envelope` (numeric needs `--envelope-source`, §4.2g) and returns a new **NOT READABLE** outcome under `--envelope unavailable` rather than falling through to `boundary_eliminated`, since a criterion that cannot fail must not be scored as passed (§4.2j); `t09_audit_starmap.py`'s `vs 0.0335` column is now `vs own envelope`, `—` unless `--envelopes` supplies one. `t10_marker_field_boundary.{md,json}` regenerated with **every measured field byte-identical** — only `envelope` (0.0335 → `null`) and `outcome` moved. The eleven affected `.md` artifacts are annotated, not regenerated: the seven post-fix seed reports get the per-metric replacement, the four `t09_audit_*.md` are **superseded outright** (one seed *and* pre-frame-fix, visible as a negative `marker_field_r`). (3) **The two-scorer finding promoted out of `envelope_correction.md`** into advisor **§4b** (peer of §4a) and close-out §6 **§4.2a-i**: every envelope was measured by a different scorer, on a different design, from the numbers it judged — tier-1 per-metric envelopes differ **2.6x–6.7x**, and **~9x** on the autocorrelation metrics, in the direction that flatters every clearance. (4) ⚠️ **The A9 retrieval failure recorded as its own finding** (advisor §4c, close-out §6, `specs/10` §4.2a-iii): its instrument-B envelope existed for a month unlooked-at because the run was **filed by its verdict rather than by what it measured**. Retrieval, not measurement — a null result's measurements outlive its verdict, and indexing a run by the question it answered makes them unfindable to anyone asking another. §4.2f one level up |
| T09 close-out | [progress/t09_inference_and_calibration.md](progress/t09_inference_and_calibration.md) — **assessment**: every comparative capability claim tested on real data is negative or unresolved; the transferable contribution is the evaluation methodology (§4.2a/b/c, the leak taxonomy, ceiling-first discipline). Carries the `morans_pearson` replication pre-registration |
| E1 | [reports/t09_zeroshot_deep.md](reports/t09_zeroshot_deep.md) — the four-arm zero-shot table. The finding is the **seen/unseen sign flip** (`lookup` wins by 0.133 where it has a row, `medcpt` by 0.300 where it does not); the primary comparison is a refutation, and the unseen half awaits a `morans_pearson` ceiling. `specs/10` §7 |
| — | [progress/durability_checkpointing.md](progress/durability_checkpointing.md) — fit checkpointing, and the bitwise-identity acceptance test that keeps Convention 3 true |
| — | [progress/decisions.md](progress/decisions.md) — spec decisions settled with no code change |

**Appending to this log:** add the entry to the task's own file under `progress/`,
update the row in the status table above, and leave this index short. The file was
split at T08 because a 2131-line PROGRESS.md could no longer be rewritten in one pass,
which is how a task's findings came to be missing from it.
